{
  "name": "@jamesclaw/blog-pipeline",
  "version": "0.1.0",
  "description": "Firebase blog pipeline — generate, optimize, deploy",
  "type": "module",
  "scripts": {
    "generate": "node src/cli.mjs generate",
    "pipeline": "node src/cli.mjs pipeline",
    "build": "node src/cli.mjs build",
    "deploy": "node src/cli.mjs deploy",
    "test": "node --test tests/**/*.test.mjs",
    "test:dry": "node src/cli.mjs pipeline --dry-run"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "playwright": "^1.59.1"
  },
  "engines": {
    "node": ">=20"
  }
}
