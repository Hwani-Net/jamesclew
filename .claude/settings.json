{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": [
    "리포 레벨 설정 — 이 리포가 마운트된 모든 Claude Code 세션에 적용됩니다.",
    "원격 클라우드 컨테이너에는 ~/.claude/ 하네스가 배포되지 않으므로(install.sh는 로컬 PC 전용),",
    "원격 세션의 토큰 사용량을 남기려면 이 파일이 유일한 경로입니다.",
    "hook은 모델의 allowed_tools와 무관하게 CLI가 실행하므로, Read 전용 routine 세션에서도 동작합니다.",
    "관측 전용이며 아무것도 차단하지 않습니다."
  ],
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/harness/hooks/token-usage-tracker.sh\"",
            "timeout": 15000
          }
        ]
      }
    ]
  }
}
