# JamesClaw 일일 자동 감사 — 2026-06-28

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-06-28  
**결과**: ⚠️ 침묵 의심 2건 — 2건 모두 **재발** (Issue #4 동일 항목, 범위 확대)

---

## 1. 사전 — 기존 OPEN Issue 조회

| No | Title | 생성일 | 댓글 수 |
|----|-------|--------|---------|
| **#4** | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 | 26 |

→ 이번 감사 결과와 비교: **2건 모두 동일 재발** (범위 확대 포함) → 기존 Issue #4에 코멘트 추가.

---

## 2. settings.json Hook 인벤토리

**등록된 Hook 이벤트 타입:**

| 이벤트 | 등록 hook 수 | 비고 |
|--------|-------------|------|
| PreToolUse | 11개 | Write/Edit·Bash·Tavily·screenshot·chrome 매처 |
| PostToolUse | 22개 | *, Bash, Write/Edit, Read, tavily, stitch, TaskCreate/TodoWrite 등 |
| SubagentStop | 1개 | verify-subagent.sh |
| UserPromptSubmit | 5개 | user-prompt.ts, declare-warn, capture-reset-times, pitfall-auto-record, build-detector |
| PreCompact | 3개 | pre-compact-snapshot, audit-session, self-evolve |
| Stop | 6개 | stop-dispatcher, session-learning, emergency-mode-check, inbox-classifier, self-evolve-trigger, pitfall-auto-record-stop |
| StopFailure | 1개 | telegram-notify stop-failure |
| PostCompact | 4개 | telegram-notify, session-start.ts, post-compact-resume, dialectic-pattern-extractor |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded / PostToolUseFailure | inline bash | 각 1개 |
| SessionStart | 8개 | telegram-notify, emergency-banner, session-start-active-infra, codex-expiry-check, session-start.ts, wiki-sync-scheduler, harness-self-audit, distress-queue-loader |

**파일 존재 검증:**

| 분류 | 대상 | 결과 |
|------|------|------|
| bash hooks | 모든 `$HOME/.claude/hooks/*.sh` 참조 59개 | ✅ harness/hooks/에 전원 존재 |
| TypeScript hooks | user-prompt.ts, session-start.ts | ✅ harness/hooks/에 존재 |
| scripts | audit-session.sh, self-evolve.sh | ✅ harness/scripts/에 존재 |

→ **침묵 의심 0건** (파일 부재 없음)

---

## 3. 의존성 매트릭스 검증 (P-113)

| 소비자 hook | 기대 state 파일 | 공급원 | path 일치 |
|-------------|----------------|--------|-----------|
| `emergency-mode-check.sh` | `${HOME}/.harness-state/5h_usage.txt` | `telegram-notify.sh` L86: `STATE_DIR="$HOME/.harness-state"` → `5h_usage.txt` | ✅ |
| `self-evolve-trigger.sh` | `$STATE_DIR/context_usage.txt` (`STATE_DIR="$HOME/.harness-state"`) | `telegram-notify.sh` L218: `context_usage.txt` | ✅ |
| `pitfall-auto-record-stop.sh` | `${HARNESS_STATE}/pitfall_pending.json` (`HARNESS_STATE="${HOME}/.harness-state"`) | `pitfall-auto-record.sh` L80: `HARNESS_STATE="${HOME}/.harness-state"` | ✅ |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-{HASH}/build_detected` (`PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)`) | `build-detector.sh` L37-42: 동일 수식 `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)`, `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` | ✅ |

→ **4/4 path 일치** (grep 직접 확인, PROJECT_HASH 산출 공식 일치 확인)

---

## 4. audit-session.sh check_ 함수 분석

**현황:**
- 헤더 명시: `현재 체크 수: 56개 (2026-05-19)`
- 마지막 업데이트: 2026-05-19 (변경 없음)
- 실제 집계: **56개** check_v 함수 (check_v119~check_v144)
- 최신 check 함수: `check_v144_model_single_session` / `check_v144_mcp_paginated_tools`

**커버리지 공백 (v2.1.145~v2.1.177 = 33개 버전):**

| 버전 | 변경 | 필요 check 함수 |
|------|------|----------------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8 default high effort + `/effort xhigh` + Dynamic Workflows (`/workflows`) | `check_v154_opus48_effort`, `check_v154_dynamic_workflows` |
| v2.1.159 | `workflow`→`ultracode` 트리거어 변경 | `check_v159_ultracode_trigger` |
| v2.1.163 | Stop/SubagentStop hook `additionalContext` + hook `if` Bash 수정 | `check_v163_hook_additional_context` |
| v2.1.166 | `fallbackModel` 배열 + cross-session `SendMessage` user 권한 분리 + `MAX_THINKING_TOKENS=0` | `check_v166_fallback_model` |
| v2.1.169 | `--safe-mode` + `/cd` + `disableBundledSkills` | `check_v169_safe_mode` |
| v2.1.170 | Claude Fable 5 GA (우리: STICKY 금지) | `check_v170_fable5_banned` |
| v2.1.172 | 서브에이전트 중첩 5단계 + 1M 컨텍스트 무크레딧 auto-compact | `check_v172_nested_subagents` |
| v2.1.176 | hook `if` Read/Edit/Write 경로 매칭 수정(v2.1.163 후속) | `check_v176_hook_if_path_fix` |
| v2.1.177 | (changelog 미게재, 로컬 매뉴얼 최신) | 검토 후 추가 |

---

## 5. 침묵 의심 목록

### ⚠️ 침묵 의심 1 (재발) — check_v144 WARN 메시지 `'d'` 키 오류

**파일**: `harness/scripts/audit-session.sh` L1020  
**현재 코드** (변경 없음):
```bash
echo "WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```

**문제**: v2.1.153에서 `d` 키 제거됨. CLAUDE.md 명시:
> "별도 `d` 키 불필요 — `d` 액션은 v2.1.153에서 제거됨"  
> "현재 세션만 바꾸려면 picker에서 `s` 키"

**CLAUDE.md 최신 정책 (v2.1.153~현재)**:
> "`/model` 호출 시 선택이 새 세션 default로 자동 저장됨. 현재 세션만 바꾸려면 picker에서 `s` 키."

**필요 수정** (L1020 교체):
```bash
echo "WARN|settings.json model 필드 미명시 — v2.1.153+ /model 영구화(원복). opusplan 고정 시 settings.json model 필드 또는 /model opusplan 1회 호출 후 자동 저장. 현재세션만 변경 시 picker 's' 키 사용."
```

---

### ⚠️ 침묵 의심 2 (재발, 범위 확대) — v2.1.145~v2.1.177 check 함수 0개

**파일**: `harness/scripts/audit-session.sh`  
**현황**: 마지막 업데이트 2026-05-19, v2.1.144까지 커버 (56개 check_).  
2026-06-28 기준 v2.1.177 배포, **33개 버전 감사 공백** (전 보고 대비 +19개 버전).

**이전 보고 대비 신규 확대 범위**:
- 2026-06-04 보고: v2.1.145~v2.1.158 (14개 버전)
- 2026-06-28 오늘: v2.1.145~v2.1.177 (33개 버전) — v2.1.159~v2.1.177 19개 추가

---

## 6. PITFALL 현황

- **총 파일 수**: 258개
- **최신 번호**: P-271 (`pitfall-271-sandbox-mount-desync-from-authoritative-file`)
- **최근 7일 신규/업데이트 (2026-06-21 ~ 2026-06-28)**:

| 번호 | 파일 | 커밋 날짜 | 종류 |
|------|------|----------|------|
| P-271 | `pitfall-271-sandbox-mount-desync-from-authoritative-file.md` | 2026-06-24 | 신규 |
| P-270 | `pitfall-270-fable5-banned-broad-rename-collateral.md` | 2026-06-21 | 신규 |
| P-262 | `pitfall-262-codex-oauth-token-expired-refresh-rotation-relogin.md` | 2026-06-22 | 업데이트 |
| P-259 | `pitfall-259-fable5-adoption-harness-diet-stale-model-anchors.md` | 2026-06-21 | 업데이트 |

→ 신규 2건: P-271(sandbox mount desync), P-270(Fable 5 금지 후 collateral rename).

---

## 7. 결론 및 액션

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 | ✅ 0건 부재 |
| 의존성 매트릭스 | ✅ 4/4 pass |
| 침묵 의심 총계 | ⚠️ 2건 (재발) |
| 신규 침묵 의심 | 0건 |
| 기존 Issue #4 상태 | ⚠️ OPEN (재발 코멘트 추가) |

**액션**: Issue #4에 재발 코멘트 추가 (범위 확대 v2.1.158→v2.1.177 명시).

---

## 한계 명시

- **정적 분석만**. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈 준수**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- v2.1.145~v2.1.177 check 함수 공백은 **런타임 동작 차단 아님** — `/audit` 결과에서 해당 버전 기능 감사 누락 의미. 로컬 PC에서 실제 기능 사용 여부는 별도 검증 필요.
