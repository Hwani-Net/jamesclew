# JamesClaw 일일 자동 감사 — 2026-07-04

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-07-04  
**결과**: ✅ 침묵 의심 **0건 CLEAR**

---

## 0. 이전 OPEN Issue 조회

harness-audit 레이블 OPEN Issue: **0건** (조회 결과 없음)

close 처리 불필요.

---

## 1. settings.json Hook 인벤토리

**등록된 Hook 이벤트 타입:**

| 이벤트 | 등록 hook 수 |
|--------|-------------|
| PreToolUse | 11개 (Write/Edit·Bash·Tavily·screenshot·chrome 매처) |
| PostToolUse | 22개 (*, Bash, Write/Edit, Read, tavily, stitch, TaskCreate/TodoWrite, PostToolUseFailure 포함) |
| SubagentStop | 1개 |
| UserPromptSubmit | 5개 |
| PreCompact | 3개 (pre-compact-snapshot, audit-session, self-evolve) |
| Stop | 6개 |
| StopFailure | 1개 |
| PostCompact | 4개 |
| SessionStart | 8개 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | inline bash (별도 파일 없음) |

**총 참조 hook 파일 수**: 62개 (sh 60개 + ts 2개)

---

## 2. Hook 파일 존재 검증

settings.json에 `$HOME/.claude/hooks/` 또는 `$HOME/.claude/scripts/`로 참조되는 모든 파일을 `harness/` 소스 경로에서 확인.

| 분류 | 결과 |
|------|------|
| bash hooks (harness/hooks/*.sh) | ✅ 58개 전원 존재 |
| TypeScript hooks (user-prompt.ts, session-start.ts) | ✅ 2개 harness/hooks/ 존재 |
| scripts (audit-session.sh, self-evolve.sh) | ✅ harness/scripts/ 전원 존재 |

침묵 의심 파일 부재 **0건**.

**참고 — harness/hooks/에 존재하지만 settings.json 미등록 파일 (정상):**
- `enforce-execution.sh` — 비활성 또는 직접 호출용
- `curation.ts` — 미등록 TypeScript
- `evidence-first.sh` — 비활성
- `test-pre-commit-conventional.sh` — 테스트 스크립트

---

## 3. 의존성 매트릭스 검증 (P-113 강제)

P-113 교훈: "기능 정상" 결론 시 path 일치까지 grep 직접 확인 의무.

| 소비자 hook | state 파일 | 공급원 | 검증 결과 |
|-------------|-----------|--------|----------|
| `emergency-mode-check.sh` | `$HARNESS_STATE/5h_usage.txt` (`HARNESS_STATE="${HOME}/.harness-state"`) | `telegram-notify.sh`: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` (`STATE_DIR="$HOME/.harness-state"`) | ✅ PATH 일치 |
| `self-evolve-trigger.sh` | `$STATE_DIR/context_usage.txt` (`STATE_DIR="$HOME/.harness-state"`) | `telegram-notify.sh`: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` (`STATE_DIR="$HOME/.harness-state"`) | ✅ PATH 일치 |
| `pitfall-auto-record-stop.sh` | `$HARNESS_STATE/pitfall_pending.json` (`HARNESS_STATE="${HOME}/.harness-state"`) | `pitfall-auto-record.sh`: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | ✅ PATH 일치 |
| `enforce-build-transition.sh` | `$STATE_DIR/build_detected` (`PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)`, `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"`) | `build-detector.sh`: 동일 PROJECT_HASH 파생 공식 + 동일 STATE_DIR 정의 | ✅ PATH + PROJECT_HASH 일치 |

4건 모두 path 일치 확인. 경로 드리프트 **0건**.

---

## 4. audit-session.sh check_ 함수 현황

| 항목 | 현황 |
|------|------|
| 총 check_ 함수 수 | **67개** (2026-04-29 기준 39개 → +28개 증가) |
| 커버 최고 버전 | `check_v176_hook_if_path_fix` (v2.1.176) |
| 현 harness 매뉴얼 버전 | v2.1.177 |
| v2.1.177 gap | changelog 미게재 → 신기능 특정 불가. 현재 감사 함수로 검출 불가 영역이나 공식 변경사항 없음으로 간주 |

누락 의심 **0건** (changelog 미게재 특성상 정적 검증 불가 한계 명시).

---

## 5. PITFALL 현황 (2026-07-04 기준)

| 항목 | 수치 |
|------|------|
| 전체 PITFALL 파일 수 | **259개** |
| 최고 번호 | pitfall-272 |
| 최근 7일 신규 항목 | **1건** |

**최근 7일 신규 PITFALL:**

| 번호 | 슬러그 |
|------|--------|
| P-272 | `expect-mcp-removed-verification-vs-navigation-misclassification` — expect MCP 완전 제거 후 검증(gstack js)과 네비게이션(claude-in-chrome) 역할 혼동 패턴 기록 |

---

## 6. 결론

**침묵 의심: 0건 CLEAR**

| 검사 항목 | 결과 |
|-----------|------|
| Hook 파일 존재 | ✅ 0건 누락 |
| 의존성 매트릭스 path 일치 (P-113) | ✅ 4/4 일치 |
| audit-session.sh 함수 커버리지 | ✅ 67개, v2.1.176까지 커버 |
| 이전 OPEN Issue | 없음 (close 처리 없음) |

---

## 7. 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성을 사전 감지하는 보완 레이어.
- **P-113 교훈 준수**: "기능 정상" 결론 시 path 일치까지 grep 직접 확인 완료.
- v2.1.177 변경사항은 changelog 미게재로 정적 검증 불가. 다음 게재 시 check_ 함수 추가 필요.
