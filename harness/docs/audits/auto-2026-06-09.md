# JamesClaw 일일 자동 감사 — 2026-06-09

**발동**: KST 09:00 (UTC 00:00) / 원격 에이전트  
**결과**: ⚠️ 침묵 의심 **2건** — 모두 Issue #4 재발  
**이전 OPEN Issue (harness-audit)**: 1건 (Issue #4) → 재발 코멘트 추가

---

## 1. 기존 OPEN Issue 조회

`harness-audit` 라벨 OPEN Issue: **1건**

| # | 제목 | 생성일 | 코멘트 수 |
|---|------|--------|---------|
| #4 | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 | 4 |

---

## 2. Hook 인벤토리 검증

settings.json 등록 hook 전체 vs `harness/hooks/` + `harness/scripts/` 실존 파일 대조.

| 분류 | hook 수 | 결과 |
|------|---------|------|
| `$HOME/.claude/hooks/` 참조 `.sh`/`.ts` | 60개 | ✅ 전원 존재 |
| `$HOME/.claude/scripts/` 참조 | 2개 (`audit-session.sh`, `self-evolve.sh`) | ✅ 전원 존재 |
| `D:/jamesclew/harness/hooks/` 참조 | 6개 (`emergency-mode-check.sh`, `inbox-classifier.sh`, `self-evolve-trigger.sh`, `emergency-banner.sh`, `pitfall-auto-record.sh`, `task-queue-sort.sh`) | ✅ 전원 존재 |
| **총계** | **68개** | **✅ 누락 0건** |

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제: writer의 STATE_DIR 정의와 reader의 STATE_DIR 정의가 PROJECT_HASH 사용 여부까지 일치하는지 grep으로 직접 확인.

| 소비자 hook | state 파일 | 공급원 | 검증 결과 |
|------------|-----------|--------|----------|
| `emergency-mode-check.sh` | `~/.harness-state/5h_usage.txt` | `telegram-notify.sh` L86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` (STATE_DIR=`$HOME/.harness-state`) | ✅ PATH 완전 일치 |
| `self-evolve-trigger.sh` | `~/.harness-state/context_usage.txt` | `telegram-notify.sh` L218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` (STATE_DIR=`$HOME/.harness-state`) | ✅ PATH 완전 일치 |
| `pitfall-auto-record-stop.sh` | `~/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` L80: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` (HARNESS_STATE=`${HOME}/.harness-state`) | ✅ PATH 완전 일치 |
| `enforce-build-transition.sh` | `~/.harness-state/build-{PROJECT_HASH}/build_detected` | `build-detector.sh` L37-42: `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` 동일 방식 | ✅ STATE_DIR + PROJECT_HASH 방식 동일 |

**결론**: 4/4 의존성 PATH 완전 일치 ✅

---

## 4. audit-session.sh check_ 함수 현황

- **현재 함수 수**: **56개** (헤더 명시: `# 현재 체크 수: 56개 (2026-05-19)`)
- **최고 버전 커버리지**: v2.1.144 (`check_v144_model_single_session`, `check_v144_mcp_paginated_tools`)
- **마지막 업데이트**: 2026-05-19

### ⚠️ 침묵 의심 #1 — check_v144 WARN 오류 가이던스 (Issue #4 재발)

`harness/scripts/audit-session.sh` L1015:
```bash
echo "WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```

- v2.1.153에서 `d` 키 제거됨 (현행 키: `s` = 현재 세션만 변경)
- 미수정 상태 지속 (2026-06-04 Issue #4 생성 이후 6일째)

### ⚠️ 침묵 의심 #2 — v2.1.145~v2.1.158 감사 공백 (Issue #4 재발)

| 상태 | 내용 |
|------|------|
| 현재 체크 함수 | 56개 (v2.1.144까지) |
| 감사 공백 버전 | v2.1.145 ~ v2.1.158+ (최소 14개 버전) |
| 미추가 check 함수 | `check_v153_model_policy_revert`, `check_v154_opus48_effort`, `check_v154_dynamic_workflows` 등 |

Issue #4 수정 체크리스트 미이행 지속. claude-code-manual.md도 동일하게 v2.1.144(2026-05-19)에서 미갱신.

---

## 5. PITFALL 현황

- **인덱스 기준 파일 수**: 50개 (P-001~P-055, 결번 5개) — `harness/docs/pitfalls/index.md` (2026-04-18 기준)
- **최근 7일 신규 항목**: 원격 환경 제약으로 로컬 `D:/jamesclew/harness/pitfalls/` 직접 확인 불가
- **참고**: 2026-06-02 감사 기준 210개 (P-223까지)

---

## 6. 부가 관찰 — 보고서 누락 기간

2026-06-04 Issue #4 생성 이후 본 저장소 main 브랜치에 감사 보고서 commit이 없음. (`git log` 확인: 마지막 commit `58a60e2` = 2026-06-02).

- 2026-06-04 ~ 2026-06-08 5일간 보고서 파일이 push되지 않은 상태
- Issue #4에 코멘트는 추가된 것으로 확인 (comments: 4)
- 원인 불명 (원격 에이전트 push 실패 가능성)
- 오늘(2026-06-09) 보고서부터 정상 push 재개

---

## 7. 결론

**침묵 의심 건수: 2건 (재발)**

| 검사 항목 | 결과 |
|----------|------|
| Hook 파일 전원 존재 (68/68) | ✅ |
| 의존성 매트릭스 path 일치 | ✅ 4/4 |
| audit-session.sh WARN 오류 가이던스 | ⚠️ 'd' 키 → 's' 키 미수정 |
| audit-session.sh 버전 커버리지 | ⚠️ v2.1.145~158+ 감사 공백 지속 |
| PITFALL DB | 원격 제약으로 로컬 수 미확인 |

재발 → Issue #4에 코멘트 추가. 신규 Issue 미생성.

---

## 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- **본 에이전트 역할**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- **원격 환경 제약**: 이 에이전트는 `harness/hooks/` 소스 기준으로 검사. 실제 배포된 `~/.claude/hooks/`는 로컬 PC에 있으며 `harness/deploy.sh` 또는 `install.sh` 실행 여부는 로컬에서만 검증 가능.
- **로컬 PITFALL 파일 불가**: `D:/jamesclew/harness/pitfalls/` 접근 불가, index.md 및 이전 보고서 기준 추정.
