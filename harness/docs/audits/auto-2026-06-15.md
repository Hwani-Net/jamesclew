# 일일 자동 감사 보고서 — 2026-06-15

> 발동: KST 09:00 (UTC 00:00) | 에이전트: Remote Scheduled Audit Agent

---

## 1. 기존 OPEN Issue 조회

| # | 제목 | 생성일 | 상태 |
|---|------|--------|------|
| [#4](https://github.com/Hwani-Net/jamesclew/issues/4) | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 | OPEN |

---

## 2. Hook 인벤토리 검증

`settings.json`에 등록된 모든 hook 이벤트 타입:

| 이벤트 | 등록 hook 수 |
|--------|-------------|
| PreToolUse | 6개 매처 (inline bash 포함) |
| SubagentStop | verify-subagent.sh |
| PostToolUse | 14개 매처 |
| UserPromptSubmit | user-prompt.ts, user-prompt-declare-warn.sh, capture-reset-times.sh, pitfall-auto-record.sh, build-detector.sh |
| PreCompact | pre-compact-snapshot.sh, audit-session.sh, self-evolve.sh |
| Stop | stop-dispatcher.sh, session-learning.sh, emergency-mode-check.sh, inbox-classifier.sh, self-evolve-trigger.sh, pitfall-auto-record-stop.sh |
| StopFailure | telegram-notify.sh |
| PostCompact | telegram-notify.sh, session-start.ts, post-compact-resume.sh, dialectic-pattern-extractor.sh |
| ConfigChange | inline bash |
| WorktreeCreate | inline bash |
| WorktreeRemove | inline bash |
| InstructionsLoaded | inline bash |
| SessionStart | telegram-notify.sh(start), emergency-banner.sh, session-start-active-infra.sh, codex-expiry-check.sh, session-start.ts, wiki-sync-scheduler.sh, harness-self-audit.sh, distress-queue-loader.sh |
| PostToolUseFailure | tool-duration-monitor.sh |

---

## 3. Hook 파일 존재 검증 (`harness/hooks/`)

전수 검사 결과 **전체 ✅ 파일 존재 확인**. 특기 사항:
- `wiki-raw-save.sh` ✅ (초기 `ls | head -60` cut-off로 누락 표시됐으나 실재 확인)
- `wiki-sync-scheduler.sh` ✅ (동일)

**파일 부재로 인한 침묵 의심: 0건**

---

## 4. 의존성 매트릭스 검증 (P-111 + P-113)

| 소비자 hook | 기대 state 파일 | 공급원 | STATE_DIR 일치 |
|-------------|----------------|--------|---------------|
| `emergency-mode-check.sh` | `~/.harness-state/5h_usage.txt` | `telegram-notify.sh` (line 86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"`) | ✅ 동일 (`$HOME/.harness-state`) |
| `self-evolve-trigger.sh` | `~/.harness-state/context_usage.txt` | `telegram-notify.sh` (line 218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"`) | ✅ 동일 (`$HOME/.harness-state`) |
| `pitfall-auto-record-stop.sh` | `~/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` (`HARNESS_STATE="${HOME}/.harness-state"`) | ✅ 동일 (`${HOME}/.harness-state`) |
| `enforce-build-transition.sh` | `~/.harness-state/build-{HASH}/build_detected` | `build-detector.sh` (`PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)`) | ✅ 동일 (PROJECT_HASH 로직 일치) |

**의존성 매트릭스 4/4 ✅ — path 불일치로 인한 침묵 의심: 0건**

---

## 5. audit-session.sh check_ 함수 현황

| 항목 | 값 |
|------|----|
| 현재 체크 수 | 56개 |
| 마지막 업데이트 | 2026-05-19 |
| 최신 check_ 함수 | `check_v144_mcp_paginated_tools` (v2.1.144) |
| 현재 Claude Code 버전 | v2.1.173 (CLAUDE.md 기준) |
| **감사 공백** | **v2.1.145 ~ v2.1.173 = 29버전** |

### 커버되지 않은 주요 버전 변경사항

| 버전 | 변경 | 필요 check_ 함수 |
|------|------|------------------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8 + `/effort xhigh` + Dynamic Workflows(`/workflows`) | `check_v154_opus48_effort`, `check_v154_dynamic_workflows` |
| v2.1.159 | 트리거어 `workflow`→`ultracode` | `check_v159_ultracode_trigger` |
| v2.1.163 | Stop/SubagentStop hook `additionalContext` + hook `if` 수정 | `check_v163_hook_additional_context` |
| v2.1.166 | `fallbackModel`(최대 3 폴백) + `MAX_THINKING_TOKENS=0` | `check_v166_fallback_model` |
| v2.1.169 | `--safe-mode` + `/cd` + `disableBundledSkills` | `check_v169_safe_mode` |
| v2.1.170 | Claude Fable 5 출시 (`claude-fable-5`) | `check_v170_fable5_model` |
| v2.1.172 | 서브에이전트 중첩 스폰(최대 5단계) + 1M auto-compact 복구 | `check_v172_subagent_nesting` |
| v2.1.173 | Fable 5 `[1m]` suffix 자동 strip | `check_v173_fable5_suffix_strip` |

---

## 6. PITFALL 현황

| 항목 | 값 |
|------|----|
| 총 PITFALL 수 | 255개 |
| 최고 번호 | pitfall-262 |

### 최근 신규 PITFALL (2026-06-04 Issue #4 이후)

| 번호 | 파일명 | 내용 요약 |
|------|--------|----------|
| P-258 | pitfall-258-openclaw-601-hooks-enabled-requires-hooks-token | OpenClaw 6.01 hooks-enabled 시 hooks 토큰 필요 |
| P-258 | pitfall-258-reins-engineering-machine-verdict-deterministic-feedback | Reins Engineering 기계 판정 + 결정론 피드백 |
| P-259 | pitfall-259-fable5-adoption-harness-diet-stale-model-anchors | Fable 5 채택 시 하네스 stale 모델 앵커 |
| P-262 | pitfall-262-codex-oauth-token-expired-refresh-rotation-relogin | Codex OAuth 토큰 만료 갱신/재로그인 |

---

## 7. 침묵 의심 결과: 2건 (재발)

⚠️ **두 항목 모두 Issue #4의 동일 항목 재발** — 미수정 상태 지속 확인.

### 침묵 의심 1: check_v144_model_single_session WARN 메시지의 'd' 키 (재발)

**파일**: `harness/scripts/audit-session.sh` line 1020

```bash
# 현재 (v2.1.153 기준 잘못됨):
"WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```

`d` 키는 v2.1.153에서 제거됨. 사용자가 이 WARN 메시지를 보면 존재하지 않는 'd' 키를 시도하게 됨.

### 침묵 의심 2: v2.1.145~v2.1.173 check_ 함수 감사 공백 (재발 + 확대)

- 2026-06-04 보고 당시: 14버전 공백 (v2.1.145~v2.1.158)
- **2026-06-15 현재: 29버전 공백 (v2.1.145~v2.1.173)** → 15버전 추가 확대
- 추가된 버전: v2.1.159, v2.1.163, v2.1.166, v2.1.169, v2.1.170, v2.1.172, v2.1.173 (7개 주요 버전 미커버)
- Fable 5 채택(STICKY), fallbackModel 정책 등 하네스 직접 영향 항목 포함

---

## 8. 조치

- 신규 침묵 의심 없음 → 신규 Issue 미생성
- 재발 2건 → Issue #4에 재발 코멘트 추가
- OPEN Issue #4: **미수정 상태 유지 (자동 close 불가)**

---

## 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- **본 에이전트**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- **git clone mtime**: 원격 환경에서 `find -newer`로 실제 신규 PITFALL 날짜 추정 불가 → 번호 기반 분류 사용.
