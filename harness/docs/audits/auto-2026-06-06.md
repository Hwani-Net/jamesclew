# JamesClaw 일일 자동 감사 — 2026-06-06

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-06-06  
**결과**: ⚠️ 침묵 의심 2건 — 2건 모두 **재발** (Issue #4 동일 항목, 3일째)

---

## 1. 사전 — 기존 OPEN Issue 조회

| No | Title | 생성일 |
|----|-------|--------|
| **#4** | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 |

→ 이번 감사 결과와 비교: **2건 모두 동일 재발** (2026-06-04 최초 발견, 2026-06-05 재발, 2026-06-06 3차 재발) → 기존 Issue #4에 코멘트 추가.

---

## 2. settings.json Hook 인벤토리

**등록된 Hook 이벤트 타입 (12종):**

| 이벤트 | 등록 hook 수 |
|--------|-------------|
| PreToolUse | 11개 hook (Write/Edit·Bash·Tavily·screenshot·chrome 매처 포함) |
| PostToolUse | 20개 hook (*, Bash, Write/Edit, Read, tavily, stitch 매처 포함) |
| SubagentStop | 1개 |
| UserPromptSubmit | 5개 |
| PreCompact | 3개 |
| Stop | 6개 |
| StopFailure | 1개 |
| PostCompact | 4개 |
| SessionStart | 8개 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded / PostToolUseFailure | 각 1개 inline |

**파일 존재 검증:**

| hook 파일 | 결과 |
|-----------|------|
| 모든 `$HOME/.claude/hooks/*.sh` 참조 | ✅ harness/hooks/에 전원 존재 |
| 모든 `$HOME/.claude/hooks/*.ts` 참조 | ✅ (session-start.ts, user-prompt.ts) |
| 모든 `$HOME/.claude/scripts/*.sh` 참조 | ✅ (audit-session.sh, self-evolve.sh) |
| `D:/jamesclew/harness/hooks/` 절대경로 6개 | ✅ harness/hooks/에 모두 존재 |

→ **침묵 의심 0건** (파일 부재 없음)

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

| 소비자 hook | 기대 state 파일 | 공급원 | path 일치 |
|-------------|----------------|--------|-----------|
| `emergency-mode-check.sh` | `$HOME/.harness-state/5h_usage.txt` | `telegram-notify.sh` → `STATE_DIR="$HOME/.harness-state"` → L86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` | ✅ |
| `self-evolve-trigger.sh` | `$HOME/.harness-state/context_usage.txt` | `telegram-notify.sh` → `STATE_DIR="$HOME/.harness-state"` → L218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` | ✅ |
| `pitfall-auto-record-stop.sh` | `$HOME/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` → `HARNESS_STATE="${HOME}/.harness-state"` → `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | ✅ |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-{HASH}/build_detected` | `build-detector.sh` → `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` — 공식 동일 | ✅ |

→ **4/4 path 일치** (PROJECT_HASH 산출 공식 grep 직접 확인 완료)

---

## 4. audit-session.sh check_ 함수 분석

**현황:**
- 헤더 명시: `현재 체크 수: 56개 (2026-05-19)`
- grep 실제 집계: **56개** (`grep -c "^check_"` 결과)
- 최신 버전 check: `check_v144_mcp_paginated_tools` / `check_v144_model_single_session` (v2.1.144)
- CLAUDE.md 현행 버전: **v2.1.158**

**커버리지 공백:**
- v2.1.145 ~ v2.1.158: **14개 버전** 감사 함수 없음
- 신규 check 미작성 항목:

| 버전 | 변경 | 필요 check 함수 |
|------|------|----------------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8 default high effort + `/effort xhigh` | `check_v154_opus48_effort` |
| v2.1.154 | Dynamic Workflows (`/workflows`) opt-in | `check_v154_dynamic_workflows` |
| v2.1.155~v2.1.158 | 미검토 | claude-code-manual.md 확인 후 추가 |

---

## 5. 침묵 의심 목록

### ⚠️ 침묵 의심 1 (재발 3회) — check_v144 WARN 메시지 `'d'` 키 오류

**파일**: `harness/scripts/audit-session.sh` (check_v144_model_single_session 함수 내)  
**현재 코드**:
```
"WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```

**문제**: v2.1.153에서 `d` 키 제거됨. CLAUDE.md 명시:
> "별도 `d` 키 불필요 — `d` 액션은 v2.1.153에서 제거됨"
> "현재 세션만 바꾸려면 picker에서 `s` 키"

**필요 수정**:
```bash
"WARN|settings.json model 필드 미명시 — v2.1.153+ /model 영구화(원복). opusplan 고정 시 settings.json model 필드 또는 /model opusplan 1회 호출 후 자동 저장. 현재세션만 변경 시 picker 's' 키 사용."
```

---

### ⚠️ 침묵 의심 2 (재발 3회) — v2.1.145~v2.1.158 check 함수 0개

**파일**: `harness/scripts/audit-session.sh`  
**현황**: 마지막 업데이트 2026-05-19, v2.1.144까지 커버. CLAUDE.md는 v2.1.158 반영.

**CLAUDE.md 감사 동기화 규칙 위반**:
> "감사 항목 동기화 필수: CLAUDE.md에 규칙 추가 또는 Claude Code 버전 업데이트 시, audit-session.sh에 대응하는 check_ 함수도 동시에 추가."

---

## 6. PITFALL 현황

- **총 파일 수**: 211개
- **최신 번호**: P-256 (`pitfall-256-deploy-claude-md-oneway-overwrite-manual-undeployed`)
- **최근 7일 신규 (2026-05-30 ~ 2026-06-06)**: P-256 1건 (2026-06-04 커밋)
  - P-256: `deploy.sh`가 CLAUDE.md를 단방향 덮어쓰기하여 로컬 편집 내용 미배포 사례

---

## 7. 결론 및 액션

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 | ✅ 0건 부재 |
| 의존성 매트릭스 | ✅ 4/4 pass |
| 침묵 의심 총계 | ⚠️ 2건 (재발 3회째) |
| 신규 침묵 의심 | 0건 |

**액션**: Issue #4에 재발(3회) 코멘트 추가.

---

## 한계 명시

- 정적 분석만. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- `D:/jamesclew/harness/hooks/` 절대경로 6개는 로컬 Windows 환경 기준이며, 원격 환경에선 Windows 경로 미검증 (파일 존재는 harness/ 소스 기준 확인).
