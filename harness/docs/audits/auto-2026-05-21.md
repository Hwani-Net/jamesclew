# 일일 자동 감사 보고서 — 2026-05-21

**발동**: KST 09:00 (UTC 00:00) 자동 감사 에이전트  
**결과**: ✅ 침묵 의심 0건 CLEAR  
**이전 OPEN Issue**: 0건 (harness-audit 라벨)

---

## 1. 사전 — 기존 OPEN Issue

```
조회 결과: OPEN Issue 0건 (harness-audit label)
→ close 처리 불필요
```

---

## 2. Hook 인벤토리 (settings.json 등록 현황)

| Hook 이벤트 | 등록 수 | 비고 |
|------------|--------|------|
| PreToolUse | 10개 | Write/Edit·Tavily·Chrome·Bash 매처별 분기 |
| PostToolUse | 22개 | Bash/Write/Edit/Read/MCP 매처별 분기 |
| UserPromptSubmit | 5개 | user-prompt.ts + 4개 sh |
| SubagentStop | 1개 | verify-subagent.sh |
| PreCompact | 3개 | snapshot + audit-session + self-evolve |
| Stop | 6개 | dispatcher + learning + D: 경로 3개 + pitfall-stop |
| StopFailure | 1개 | telegram-notify.sh stop-failure |
| PostCompact | 4개 | telegram + session-start.ts + resume + dialectic |
| SessionStart | 9개 | telegram + banner + infra + codex + session-start.ts + wiki-sync + self-audit + distress |
| PostToolUseFailure | 1개 | tool-duration-monitor.sh |
| ConfigChange | 1개 | inline echo → config_changes.log |
| WorktreeCreate/Remove | 2개 | inline echo → worktree.log |
| InstructionsLoaded | 1개 | CLAUDE.md hash 변경 감지 |

**총 등록 hook 명령: 66개**

---

## 3. 파일 존재 검증

모든 settings.json 참조 hook 파일 ✅ 존재 확인.

### $HOME/.claude/hooks/ (55개 파일)
모두 harness/hooks/에 존재 ✅

### D:/jamesclew/harness/hooks/ (6개 파일 — Windows 절대경로)
| 파일 | 존재 여부 |
|------|---------|
| emergency-banner.sh | ✅ |
| emergency-mode-check.sh | ✅ |
| inbox-classifier.sh | ✅ |
| self-evolve-trigger.sh | ✅ |
| pitfall-auto-record.sh | ✅ |
| task-queue-sort.sh | ✅ |

### $HOME/.claude/scripts/ (2개 파일)
| 파일 | 존재 여부 |
|------|---------|
| audit-session.sh | ✅ (harness/scripts/) |
| self-evolve.sh | ✅ (harness/scripts/) |

**파일 부재 건수: 0건 ✅**

---

## 4. 의존성 매트릭스 검증 (P-113 강제)

writer와 reader의 STATE_DIR + 파일명 직접 grep 검증.

### 4-1. emergency-mode-check.sh ← telegram-notify.sh
| 항목 | writer (telegram-notify.sh) | reader (emergency-mode-check.sh) |
|------|-----------------------------|----------------------------------|
| STATE_DIR 정의 | `STATE_DIR="$HOME/.harness-state"` (L9) | `HARNESS_STATE="${HOME}/.harness-state"` (L8) |
| 파일 경로 | `$STATE_DIR/5h_usage.txt` (L86) | `${HARNESS_STATE}/5h_usage.txt` (L9) |
| PROJECT_HASH 사용 | ❌ (없음) | ❌ (없음) |
| **판정** | ✅ **PATH 일치** | |

### 4-2. self-evolve-trigger.sh ← telegram-notify.sh
| 항목 | writer (telegram-notify.sh) | reader (self-evolve-trigger.sh) |
|------|-----------------------------|----------------------------------|
| STATE_DIR 정의 | `STATE_DIR="$HOME/.harness-state"` (L9) | `STATE_DIR="$HOME/.harness-state"` (L9) |
| 파일 경로 | `$STATE_DIR/context_usage.txt` (L218) | `CONTEXT_FILE="$STATE_DIR/context_usage.txt"` (L11) |
| PROJECT_HASH 사용 | ❌ (없음) | ❌ (없음) |
| **판정** | ✅ **PATH 일치** | |

### 4-3. pitfall-auto-record-stop.sh ← pitfall-auto-record.sh
| 항목 | writer (pitfall-auto-record.sh) | reader (pitfall-auto-record-stop.sh) |
|------|----------------------------------|---------------------------------------|
| STATE_DIR 정의 | `HARNESS_STATE="${HOME}/.harness-state"` (L25) | `HARNESS_STATE="${HOME}/.harness-state"` (L28) |
| 파일 경로 | `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` (L82) | `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` (L29) |
| PROJECT_HASH 사용 | ❌ (없음) | ❌ (없음) |
| **판정** | ✅ **PATH 일치** | |

### 4-4. enforce-build-transition.sh ← build-detector.sh
| 항목 | writer (build-detector.sh) | reader (enforce-build-transition.sh) |
|------|---------------------------|---------------------------------------|
| PROJECT_HASH 공식 | `$(echo "$PWD" \| md5sum \| cut -c1-8)` (L37) | `$(echo "$PWD" \| md5sum \| cut -c1-8)` (L14) |
| STATE_DIR 정의 | `"$HOME/.harness-state/build-$PROJECT_HASH"` (L38) | `"$HOME/.harness-state/build-$PROJECT_HASH"` (L15) |
| 파일 경로 | `$STATE_DIR/build_detected` (L42) | `$STATE_DIR/build_detected` (L38) |
| **판정** | ✅ **PATH + PROJECT_HASH 공식 일치** | |

**의존성 매트릭스 침묵 의심: 0건 ✅**

---

## 5. audit-session.sh check_ 함수 현황

| 항목 | 값 |
|------|----|
| 현재 check_ 함수 수 | **56개** |
| 이전 기준 (2026-04-29) | 39개 |
| 신규 추가 | +17개 |

### 최신 추가 함수 (v2.1.133~v2.1.144 대응)
```
check_v133_worktree_baseref
check_v133_claude_effort_env
check_v136_hard_deny
check_v139_goal_agentview
check_v139_hook_args_exec
check_v141_terminal_sequence
check_v142_agents_flags
check_v142_fast_mode_opus47
check_v143_stop_hook_block_cap
check_v143_powershell_policy
check_v144_model_single_session
check_v144_mcp_paginated_tools
check_v119_config_persistence
check_design
```

**Claude Code v2.1.144 대응 상태: ✅ 완료**  
(최신 기능 — `/model` 단일세션 정책, MCP paginated tools fix, Stop hook 8-cap 모두 커버)

---

## 6. PITFALL 현황

| 항목 | 값 |
|------|----|
| 총 pitfall 파일 수 | **170개** |
| 최고 번호 | P-173 |

### 최근 7일 신규 등록 (2026-05-14~2026-05-21, git 커밋 기준)
| Pitfall | 슬러그 | 커밋 |
|---------|--------|------|
| P-130 | silent-stderr-spawn-debugging | b43367b |
| P-130 | video-format-text-slideshow | b43367b |
| P-131 | asset-relevance-legal-risk | b43367b |
| P-131 | korean-ui-english-menu-mismatch | b43367b |
| P-132 | character-static-overuse-subtitle-size | d34897c |
| P-132 | shopify-dev-dashboard-client-credentials-grant | d34897c |
| P-133 | connect-ai-agent-self-coding-limitation | 974b239 |
| P-133 | sadtalker-anime-asymmetry | 974b239 |
| P-134 | hook-timeout-not-propagated-to-child | 974b239 |
| P-134 | liveportrait-audio-unsupported | 974b239 |
| P-135 | connect-ai-llm-response-surrogate-pair-broken | 974b239 |
| P-135 | sonnet-vision-symmetry-blindness | 974b239 |
| P-171 | gbrain-config-set-secret-leak | 90e2447 |
| P-172 | handoff-failure-gbrain-revival | 867b1d9 |
| P-173 | settings-template-auto-revert | 0a8dc9f |

**7일 신규: 15파일 (9개 번호)**

---

## 7. 최종 판정

| 점검 항목 | 결과 |
|----------|------|
| Hook 파일 부재 | ✅ 0건 |
| 의존성 매트릭스 path 불일치 | ✅ 0건 |
| audit-session.sh 최신 버전 대응 | ✅ 완료 (v2.1.144) |
| 기존 OPEN Issue | ✅ 0건 |

**침묵 의심 0건 — CLEAR ✅**

---

## 한계 명시

- 정적 분석만. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- D:/jamesclew/harness/hooks/ 경로는 Windows 로컬 경로. 원격 컨테이너에서 실제 실행 불가능하나 파일 존재(리포 기준) 확인으로 대체.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 본 보고서에서 grep 직접 확인 완료.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
