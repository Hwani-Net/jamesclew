# 일일 자동 감사 보고서 — 2026-07-06

**실행 시각**: 2026-07-06 KST 09:00 (UTC 00:00)  
**감사 에이전트**: Remote Claude Code (harness-audit-daily)  
**결과**: ⚠️ 침묵 의심 1건 발견

---

## 1. Hook 인벤토리 검증

`settings.json` → `harness/hooks/` 파일 존재 대조

| 이벤트 | 등록 hook 수 | 파일 존재 | 누락 |
|--------|-------------|-----------|------|
| PreToolUse | 11개 | ✅ 전부 | 0건 |
| SubagentStop | 1개 | ✅ | 0건 |
| PostToolUse | 20개 | ✅ 전부 | 0건 |
| UserPromptSubmit | 5개 | ✅ 전부 | 0건 |
| PreCompact | 3개 (+ scripts) | ✅ | 0건 |
| Stop | 5개 | ✅ 전부 | 0건 |
| StopFailure | 1개 | ✅ | 0건 |
| PostCompact | 4개 | ✅ 전부 | 0건 |
| SessionStart | 8개 | ✅ 전부 | 0건 |
| PostToolUseFailure | 1개 | ✅ | 0건 |
| **합계** | **58개 참조** | **✅ 전부** | **0건** |

`harness/hooks/` 총 파일 수: 61개 (session-start.ts, user-prompt.ts, curation.ts 포함)

---

## 2. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제 조건: writer/reader의 STATE_DIR + PROJECT_HASH 사용 여부 동시 일치 확인.

| 소비자 hook | 기대 state 파일 | 공급원 | 검증 결과 |
|-------------|----------------|--------|----------|
| `emergency-mode-check.sh` | `$HOME/.harness-state/5h_usage.txt` | `telegram-notify.sh` line 86 | ✅ STATE_DIR 일치 (`$HOME/.harness-state`) |
| `self-evolve-trigger.sh` | `$HOME/.harness-state/context_usage.txt` | `telegram-notify.sh` line 218 | ✅ STATE_DIR 일치 (`$HOME/.harness-state`) |
| `pitfall-auto-record-stop.sh` | `$HOME/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` line 80 | ✅ HARNESS_STATE 일치 (`$HOME/.harness-state`) |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-{PROJECT_HASH}/build_detected` | `build-detector.sh` | ✅ STATE_DIR + PROJECT_HASH 둘 다 일치 (`md5sum` 동일 방식) |

**4쌍 전부 PASS** — "같은 파일명 = 같은 path" 이외 path 일치까지 검증 완료.

---

## 3. audit-session.sh check_ 함수 현황

- **현재 함수 수**: 67개 (2026-04-29 기준 39개 → 28개 증가)
- **최신 버전 커버리지**: `check_v176_hook_if_path_fix` (Claude Code v2.1.176)
- **v2.1.177**: changelog 미게재 → 검증 대상 없음 (정상)

### ⚠️ 침묵 의심 1건: `check_v170_fable5_banned` 로직 스탈

```
위치: harness/scripts/audit-session.sh
함수: check_v170_fable5_banned()
```

**현재 동작**:
- settings.json에 Fable 5가 없으면 `PASS|Fable 5 미사용 — v2.1.170 STICKY 금지 준수 (재도입 금지)` 출력
- settings.json에 Fable 5가 있으면 `FAIL|STICKY DECISIONS 위반! 2026-06-21 대표님 금지 지시` 오경보

**STICKY DECISIONS 실제 상태 (2026-07-03 기준)**:
- `claude-fable-5` 금지 해제 확인 — 대표님 명시: "의도적 재도입이야. fable 5 모델이 되살아났기 때문이야."
- 메인 세션 모델 = `claude-fable-5`, 폴백 = `claude-opus-4-8`

**영향**:
- 올바른 정책(Fable 5 메인 설정)을 audit FAIL로 오보 → 잘못된 롤백 유도 가능
- "재도입 금지" 메시지가 PASS에도 노출 → 잘못된 정책 신호 지속 전파
- 감사 함수가 STICKY 결정과 역전되어 **감사 무결성 침묵** 발생

**필요 수정**:
```bash
# check_v170_fable5_banned → check_v170_fable5_reinstated 으로 갱신
# 2026-07-03 대표님 금지 해제 결정 반영
# Fable 5 존재 = PASS, 미존재 = INFO (권고)
```

---

## 4. PITFALL 현황

- **총 개수**: 259개
- **최근 7일 신규**: 1건
  - `pitfall-272-expect-mcp-removed-verification-vs-navigation-misclassification.md` (2026-07-02)
  - 내용: expect MCP 완전 제거(2026-07-01 대표님 승인), 렌더 검증 gstack js 이관, navigation vs verification 분류 오류 기록

---

## 5. 기타 관찰사항

- **OpenClaw 폐기 (2026-07-04)**: 전 gateway + 봇 10개 완전 종료. OpenClaw 관련 hook/audit 대상 없음. 리스크 감소.
- **settings.json `model` 필드**: 미지정 상태. `fallbackModel: ["claude-sonnet-4-6"]`. Fable 5 메인 설정은 로컬 PC `/model` 명령으로 관리되므로 harness settings.json 비반영 정상.

---

## 6. 결론 및 조치

| 항목 | 상태 |
|------|------|
| Hook 파일 전수검사 | ✅ CLEAR |
| 의존성 매트릭스 (P-113) | ✅ CLEAR |
| audit-session.sh 신버전 커버 | ✅ CLEAR (v2.1.176까지) |
| PITFALL 신규 7일 | 1건 정상 기록됨 |
| **침묵 의심** | ⚠️ **1건 — check_v170_fable5_banned 스탈** |

**조치**: GitHub Issue 생성 (신규, 기존 OPEN Issue 없음)

---

## 한계 명시

- **정적 분석만**. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 → 이번 감사에서 적용 완료.
- OpenClaw 관련 hooks/config는 폐기 완료로 더이상 감사 대상 아님.
