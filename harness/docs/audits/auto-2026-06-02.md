# JamesClaw 일일 자동 감사 — 2026-06-02

**발동**: KST 09:00 (UTC 00:00) / 원격 에이전트  
**결과**: ✅ 침묵 의심 **0건** CLEAR  
**이전 OPEN Issue (harness-audit)**: 0건 → close 불필요

---

## 1. 기존 OPEN Issue 조회

`harness-audit` 라벨 OPEN Issue: **0건** (GitHub MCP `list_issues` 확인)

---

## 2. Hook 인벤토리 검증

settings.json 등록 hook 전체 vs `harness/hooks/` + `harness/scripts/` 실존 파일 대조.  
Python 스크립트로 전 항목 자동 비교 (`json.load` → `re.search` → `os.listdir` 교차).

> **주의**: `ls | head -60` 방식은 w* 파일군(`watchdog-ralph.sh`, `wiki-raw-save.sh`, `wiki-sync-scheduler.sh`)을 잘라낼 수 있음. 이번 감사부터 `find` + Python 전수 비교 방식으로 고정.

| 분류 | hook 수 | 결과 |
|------|---------|------|
| `$HOME/.claude/hooks/` 참조 | 60개 | ✅ 전원 존재 |
| `$HOME/.claude/scripts/` 참조 | 2개 (`audit-session.sh`, `self-evolve.sh`) | ✅ 전원 존재 |
| `D:/jamesclew/harness/hooks/` 참조 | 5개 (`emergency-mode-check.sh`, `inbox-classifier.sh`, `self-evolve-trigger.sh`, `emergency-banner.sh`, `pitfall-auto-record.sh`, `task-queue-sort.sh`) | ✅ 전원 존재 |
| **총계** | **67개** | **✅ 누락 0건** |

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제: writer의 STATE_DIR 정의와 reader의 STATE_DIR 정의가 PROJECT_HASH 사용 여부까지 일치하는지 grep으로 직접 확인.

| 소비자 hook | state 파일 | 공급원 | 검증 결과 |
|------------|-----------|--------|----------|
| `emergency-mode-check.sh` | `~/.harness-state/5h_usage.txt` | `telegram-notify.sh` L86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` (STATE_DIR=`$HOME/.harness-state`) | ✅ PATH 완전 일치 |
| `self-evolve-trigger.sh` | `~/.harness-state/context_usage.txt` | `telegram-notify.sh` L218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` (STATE_DIR=`$HOME/.harness-state`) | ✅ PATH 완전 일치 |
| `pitfall-auto-record-stop.sh` | `~/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` L80: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` (HARNESS_STATE=`${HOME}/.harness-state`) | ✅ PATH 완전 일치 |
| `enforce-build-transition.sh` | `~/.harness-state/build-{PROJECT_HASH}/build_detected` | `build-detector.sh` L37-42: `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` 동일 방식 | ✅ STATE_DIR + PROJECT_HASH 방식 동일 |

**결론**: 4/4 의존성 PATH 완전 일치 ✅

---

## 4. audit-session.sh check_ 함수 현황

- **현재 함수 수**: **56개** (기준일 2026-04-29: 39개 → +17개 증가)
- v2.1.144까지 신기능 대응 완료:
  - `check_v144_model_single_session` — `/model` 단일 세션 정책 변화 (v2.1.144)
  - `check_v144_mcp_paginated_tools` — MCP 페이지네이션 도구 (v2.1.144)
  - `check_v143_stop_hook_block_cap`, `check_v143_powershell_policy`
  - `check_v142_agents_flags`, `check_v142_fast_mode_opus47`
  - `check_v141_terminal_sequence`, `check_v139_goal_agentview`, `check_v139_hook_args_exec`
  - `check_v136_hard_deny` 등 총 17건 추가

**Claude Code 신버전 누락 여부**: v2.1.144 커버리지 완비. 누락 없음 ✅

---

## 5. PITFALL 현황

- **총 PITFALL 수**: **210개** (pitfall-001 ~ pitfall-229, 중간 결번 포함)
- **최근 7일 신규 항목** (2026-05-26~ commit `9990626` 기준, 배치 추가):

| # | 슬러그 요약 |
|---|------------|
| P-176~181 | 리넘버링 (구 P-130~135): 영상 포맷·자산·캐릭터·LivePortrait·Vision 관련 |
| P-182~188 | AniTalker·LatentSync·OpenClaw Windows/WSL2 배포 관련 6건 |
| P-189~200 | OpenClaw 봇 루프·Codex 위임·채널 라우팅·context 리프레시 등 12건 |
| P-201~222 | 채널 분리·결재·벤치마킹·Hybrid Sync·Discord 피드백·WSL2 autostart 등 22건 |
| P-223 | WSL2 vmIdleTimeout 무한 + OpenClaw boot autostart |

> 최근 7일 신규 PITFALL 주요 카테고리: OpenClaw 운영 패턴(채널 분리·cron 신뢰성·사용자 결정), Hybrid Sync 아키텍처(WSL2↔Windows), Vercel 배포 관련.

---

## 6. 결론

**침묵 의심 건수: 0건 — CLEAR**

| 검사 항목 | 결과 |
|----------|------|
| Hook 파일 전원 존재 (67/67) | ✅ |
| 의존성 매트릭스 path 일치 | ✅ 4/4 |
| audit-session.sh 버전 커버리지 | ✅ v2.1.144까지 |
| PITFALL DB 무결성 | ✅ 210개 |

이전 OPEN Issue 없음 → close 불필요. 텔레그램 미발송. 신규 Issue 미생성.

---

## 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- **본 에이전트 역할**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료.
- **원격 환경 제약**: 이 에이전트는 `harness/hooks/` 소스 기준으로 검사. 실제 배포된 `~/.claude/hooks/`는 로컬 PC에 있으며 `harness/deploy.sh` 또는 `install.sh` 실행 여부는 로컬에서만 검증 가능.
- **ls 잘림 주의**: `ls | head -N` 방식 대신 `find + Python 전수 비교`를 사용해야 w* 파일군 누락 방지.
