# Harness Daily Audit — 2026-05-04 (자동 생성)

> 발동: KST 09:00 (UTC 00:00) / 에이전트: JamesClaw Harness Daily Audit Agent  
> 기준 커밋: `e8f6a36` (2026-04-29)

---

## 요약

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 검증 | ✅ 49/49 전부 존재 |
| 의존성 매트릭스 검증 | ❌ **3건 침묵 의심** |
| audit-session.sh check_ 개수 | ✅ 39개 (예상치 일치) |
| 피트폴 총 개수 | ✅ 68개 |
| 최근 7일 신규 피트폴 | P-072, P-073 (2건) |

---

## 1. Hook 파일 존재 검증

settings.json에 등록된 모든 hook 명령 대상 파일(49개)을 harness/hooks/와 대조.

**결과: 전부 존재 (이상 없음)**

```
OK: verify-memory-write.sh, enforce-build-transition.sh, tavily-guardrail.sh,
    vision-routing-guard.sh, chrome-read-page-guard.sh, quality-gate.sh,
    pre-commit-conventional.sh, irreversible-alert.sh, bash-tool-blocker.sh,
    verify-deploy.sh, verify-subagent.sh, periodic-audit.sh, enforce-review.sh,
    error-telegram.sh, loop-detector.sh, timeline-logger.sh, post-edit-dispatcher.sh,
    regression-autotest.sh, enforce-cross-review.sh, change-tracker.sh,
    regression-guard.sh, test-manipulation-guard.sh, read-once.sh,
    sonnet-vision-delegate-guard.sh, explore-router.sh, log-filter.sh,
    cost-tracker.sh, wiki-raw-save.sh, progressive-summarize.sh, stitch-drift-guard.sh,
    watchdog-ralph.sh, user-prompt-declare-warn.sh, capture-reset-times.sh,
    pre-compact-snapshot.sh, stop-dispatcher.sh, session-learning.sh,
    telegram-notify.sh, wiki-sync-scheduler.sh, codex-expiry-check.sh,
    copilot-api-autostart.sh, post-compact-resume.sh, task-queue-sort.sh,
    pitfall-auto-record.sh, emergency-mode-check.sh, inbox-classifier.sh,
    self-evolve-trigger.sh, emergency-banner.sh,
    user-prompt.ts, curation.ts, session-start.ts
```

---

## 2. 의존성 매트릭스 검증 (P-111 기준)

### ❌ 침묵 의심 #1 — `5h_usage.txt` 공급원 코드 부재 [HIGH]

- **소비자**: `emergency-mode-check.sh` line 9 → `HARNESS_STATE/5h_usage.txt` 읽음
- **절차 명시 공급원**: `telegram-notify.sh` → `save_last_usage()` 직후 line ~89에서 echo 1줄
- **검증 결과**: `save_last_usage()` (line 39~50)는 `$STATE_DIR/last_usage.json`만 씀. `5h_usage.txt` 생성 코드는 telegram-notify.sh 및 전체 harness/hooks/ 어디에도 없음 (`grep -rn "5h_usage" hooks/ | grep -v emergency-mode-check` → 결과 없음)
- **영향**: emergency-mode-check.sh는 항상 "FILE NOT FOUND → skip" 분기 진입. 비상 모드 검사 기능 완전 침묵.

### ❌ 침묵 의심 #2 — `context_usage.txt` 공급원 코드 부재 [HIGH]

- **소비자**: `self-evolve-trigger.sh` line 11 → `STATE_DIR/context_usage.txt` 읽음
- **절차 명시 공급원**: `telegram-notify.sh` → `get_context()` 내부 line ~225에서 echo 1줄이 파일에 씌여야 함
- **검증 결과**: `get_context()` (line 208~)는 transcript에서 값을 읽어 stdout echo만 수행. 파일 write 없음. 전체 hooks/에서 `context_usage.txt` 생성 코드 없음 (`grep -rn "context_usage" hooks/ | grep -v self-evolve-trigger` → 결과 없음)
- **영향**: `FAKE_CONTEXT_PCT` / `CLAUDE_CONTEXT` 환경변수가 없으면 self-evolve-trigger.sh의 컨텍스트 임계 조건이 항상 skip. 자가진화 트리거 침묵.

### ❌ 침묵 의심 #3 — `pitfall-auto-record-stop.sh` 파일 부재 + Stop hook 미등록 [MEDIUM]

- **절차 명시 역할**: pitfall-auto-record.sh가 1차 게이트로 `pitfall_pending.json` 생성 → pitfall-auto-record-stop.sh가 Stop hook에서 2단계 처리
- **검증 결과**:
  - `pitfall-auto-record-stop.sh` 파일 자체가 harness/hooks/에 없음
  - settings.json Stop hook에도 미등록
  - pitfall-auto-record.sh 내부에서 `pitfall_pending.json` 생성 코드도 없음 (grep 결과 없음)
- **영향**: pitfall 2단계 게이트 기능 미동작. 자동 pitfall 기록 Stop 경유 흐름 없음.

### ℹ️ 정보 #4 — `build-detector.sh` 파일 부재 (기능 정상)

- **절차 기대**: "build-detector.sh가 UserPromptSubmit에서 build_detected 생성"
- **실제**: `user-prompt.ts` line 376에서 `build_detected` 파일 생성. `session-start.ts` line 117에서 정리.
- **판정**: 기능 정상 동작. build-detector.sh는 user-prompt.ts에 통합된 것으로 추정. 절차 문서 불일치 (문서 업데이트 권장).

---

## 3. audit-session.sh check_ 함수 개수

- **현재**: 39개 ✅ (2026-04-29 기준 예상치 일치)
- **버전 커버리지**: v2.1.119 (check_v119_config_persistence), v2.1.120 (check_v120_powershell_fallback), v2.1.121 (check_v121_post_tool_output, check_v121_mcp_always_load)
- **v2.1.122~v2.1.123**: 로컬 매뉴얼(claude-code-manual.md)에 해당 버전 항목 없음. 공급원 changelog.md도 v2.1.121까지만 기술. 신규 check_ 추가 필요 여부 판단 불가 (정적 분석 한계 — 런타임 환경의 `~/.claude/cache/changelog.md` 접근 불가).

---

## 4. 피트폴 현황

| 항목 | 값 |
|------|-----|
| 총 피트폴 파일 수 | 68개 |
| 번호 범위 | P-001 ~ P-073 (결손: P-014, P-015, P-045, P-046, P-047) |

### 최근 7일 신규/변경 (git 기준)

| 날짜 | 번호 | 제목 | 상태 |
|------|------|------|------|
| 2026-04-27 | P-072 | user-email-vs-device-account-confusion | 신규 |
| 2026-04-27 | P-073 | ghost-mode-halkaYo-pattern | 신규 |
| 2026-04-29 | P-067 | claude-code-2120-resume-fkh-undefined | resolved 마킹 |

---

## 5. 침묵 의심 종합

**총 3건** (HIGH 2건, MEDIUM 1건)

| # | 심각도 | 항목 | 영향 hook |
|---|--------|------|----------|
| 1 | HIGH | 5h_usage.txt 공급원 없음 | emergency-mode-check.sh |
| 2 | HIGH | context_usage.txt 공급원 없음 | self-evolve-trigger.sh |
| 3 | MEDIUM | pitfall-auto-record-stop.sh 부재+미등록 | Stop hook 2단계 |

---

## 한계 명시

- **정적 분석만 수행**. 파일 존재·코드 grep 기반. 런타임 실행 결과는 검증하지 않음.
- 로컬 PC SessionStart 자동 hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- `~/.claude/cache/changelog.md` 접근 불가 → v2.1.122-123 신기능 check_ 필요 여부 판단 불가.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
