# Harness Daily Audit — 2026-05-05 (자동 생성)

> 발동: KST 09:00 (UTC 00:00) / 에이전트: JamesClaw Harness Daily Audit Agent  
> 기준 커밋: `6e7336f` (2026-05-05 기준 HEAD)  
> Claude Code 버전: v2.1.128  
> 이전 보고서: `auto-2026-05-04.md` (침묵 의심 0건 CLEAR)

---

## 요약

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 검증 | ✅ 전부 존재 |
| 의존성 매트릭스 검증 (4건) | ✅ 전부 공급원 확인 |
| audit-session.sh check_ 개수 | ✅ 39개 (예상치 일치) |
| 피트폴 총 개수 | ✅ 96개 (어제 대비 +2) |
| 최근 7일 신규 피트폴 | P-113, P-114, P-115 (3건) |
| **침묵 의심 종합** | **✅ 0건** |

---

## 1. Hook 파일 존재 검증

settings.json 등록 hook 전수 대조 결과 — 전부 harness/hooks/ 또는 harness/scripts/ 내 존재.

```
$HOME/.claude/hooks/ 계열 (harness/hooks/ 보관):
  verify-memory-write.sh, enforce-build-transition.sh, tavily-guardrail.sh,
  vision-routing-guard.sh, chrome-read-page-guard.sh, quality-gate.sh,
  pre-commit-conventional.sh, irreversible-alert.sh, bash-tool-blocker.sh,
  verify-deploy.sh, verify-subagent.sh, periodic-audit.sh, enforce-review.sh,
  error-telegram.sh, loop-detector.sh, timeline-logger.sh, post-edit-dispatcher.sh,
  regression-autotest.sh, enforce-cross-review.sh, change-tracker.sh,
  regression-guard.sh, test-manipulation-guard.sh, read-once.sh,
  sonnet-vision-delegate-guard.sh, explore-router.sh, log-filter.sh,
  cost-tracker.sh, wiki-raw-save.sh, progressive-summarize.sh, stitch-drift-guard.sh,
  watchdog-ralph.sh, user-prompt-declare-warn.sh, capture-reset-times.sh,
  pre-compact-snapshot.sh, stop-dispatcher.sh, session-learning.sh,
  pitfall-auto-record-stop.sh, telegram-notify.sh, wiki-sync-scheduler.sh,
  codex-expiry-check.sh, copilot-api-autostart.sh, post-compact-resume.sh,
  harness-self-audit.sh, build-detector.sh

TypeScript hooks:
  user-prompt.ts, curation.ts, session-start.ts

D:/jamesclew/harness/hooks/ 직접 참조:
  task-queue-sort.sh, pitfall-auto-record.sh, emergency-mode-check.sh,
  inbox-classifier.sh, self-evolve-trigger.sh, emergency-banner.sh

$HOME/.claude/scripts/ 참조:
  audit-session.sh, self-evolve.sh
```

---

## 2. 의존성 매트릭스 검증 (P-111 + P-113 기준)

### ✅ 의존성 #1 — `5h_usage.txt` 공급원

- **소비자**: `emergency-mode-check.sh` → `${HOME}/.harness-state/5h_usage.txt` (HARNESS_STATE + 파일명)
- **공급원**: `telegram-notify.sh` line 90 → `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` (STATE_DIR=`$HOME/.harness-state`)
- **상태**: ✅ PATH 일치. PROJECT_HASH 미사용 (단일 flat 경로) — 양쪽 동일.

### ✅ 의존성 #2 — `context_usage.txt` 공급원

- **소비자**: `self-evolve-trigger.sh` → `$STATE_DIR/context_usage.txt` (STATE_DIR=`$HOME/.harness-state`)
- **공급원**: `telegram-notify.sh` line 236 → `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` (동일 STATE_DIR)
- **상태**: ✅ PATH 일치. PROJECT_HASH 미사용 — 양쪽 동일.

### ✅ 의존성 #3 — `pitfall_pending.json` 공급원

- **소비자**: `pitfall-auto-record-stop.sh` → `${HOME}/.harness-state/pitfall_pending.json` (HARNESS_STATE 변수)
- **공급원**: `pitfall-auto-record.sh` line 82 → `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` (동일 변수명·경로)
- **상태**: ✅ PATH 일치.

### ✅ 의존성 #4 — `build_detected` 공급원

- **소비자**: `enforce-build-transition.sh` → `PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)` + `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` + `$STATE_DIR/build_detected`
- **공급원**: `build-detector.sh` → `PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)` + `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` + `DETECTED_FILE="$STATE_DIR/build_detected"`
- **상태**: ✅ PATH 일치. md5sum 공식 + PROJECT_HASH 사용 여부 모두 동일. P-113 교훈 준수.

---

## 3. audit-session.sh check_ 함수 개수

| 항목 | 값 |
|------|-----|
| 현재 check_ 함수 수 | **39개** |
| baseline (2026-04-29) | 39개 |
| 차이 | 0 |
| 최신 버전별 check | `check_v121_post_tool_output`, `check_v121_mcp_always_load` |
| 현재 Claude Code 버전 | v2.1.128 |

**버전 커버리지 분석**:

| 버전 | 로컬 매뉴얼 | 하네스 영향 | check_ 필요 |
|------|-------------|-------------|-------------|
| v2.1.119~v2.1.123 | 문서화 완료 (이전 감사 확인) | 없음 (UI/Bedrock/OAuth) | 불필요 |
| v2.1.124~v2.1.126 | ❓ 미반영 (어제 보고서 동일 한계) | 판단 불가 | 판단 불가 |
| v2.1.127~v2.1.128 | ❓ 미반영 (신규 버전) | 판단 불가 | 판단 불가 |

> ℹ️ **정보 (정적 분석 한계)**: v2.1.124~v2.1.128은 로컬 매뉴얼 미반영. 원격 환경에서 `~/.claude/cache/changelog.md` 접근 불가. 어제(v2.1.126 기준) 동일 한계 이어짐. 런타임 환경에서 `~/.claude/cache/changelog.md` 확인 및 check_ 추가 여부 검토 권장.

---

## 4. 피트폴 현황

| 항목 | 값 |
|------|-----|
| 총 피트폴 파일 수 | **96개** |
| 어제 대비 증가 | +2 (94 → 96) |
| 번호 범위 최상위 | P-115 |

### 최근 7일 신규 (2026-04-29~2026-05-05)

| 날짜 | 번호 | 제목 |
|------|------|------|
| 2026-05-04 | P-113 | path-mismatch-dead-code — `user-prompt.ts` build_detected 경로 불일치 발견·수정 |
| 2026-05-04 | P-114 | state-file-freshness-not-verified — stale 상태 파일 신뢰, 컨텍스트 % 오보 |
| 2026-05-04 | P-115 | repl-only-slash-command-not-cli-automatable — `/compact` 외부 자동화 불가, `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` 대체 |

> P-113은 어제 보고서에서 이미 확인. P-114, P-115는 어제 감사 이후 추가된 신규 항목.

---

## 5. 침묵 의심 종합

**침묵 의심: 0건**

정적 분석 범위 내 모든 확인 항목 이상 없음.
이전 OPEN Issue: 없음 (harness-audit 라벨 기준).

---

## 한계 명시

- **정적 분석만 수행**. 파일 존재·코드 grep 기반. 런타임 실행 결과는 검증하지 않음.
- 로컬 PC SessionStart 자동 hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- `~/.claude/cache/changelog.md` 미접근(클라우드 환경) → v2.1.124~v2.1.128 하네스 영향 판단 불가. 로컬 매뉴얼(`~/.claude/docs/claude-code-manual.md`) 업데이트 및 해당 버전 check_ 검토는 수동 조치 권장.
- **본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어**. P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 오늘 의존성 #4에서 검증 완료.
