# JamesClaw 일일 자동 감사 — 2026-07-03

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-07-03  
**결과**: ✅ 침묵 의심 **0건 CLEAR** + Issue #10 **오탐 확인 → close**

---

## 0. 이전 OPEN Issue 조회

| 번호 | 제목 | 상태 |
|------|------|------|
| #10 | `[harness-audit-daily] 2026-07-02: 1건 침묵 의심 — PreCompact self-evolve.sh 경로 불일치` | OPEN |

→ 이번 감사에서 **오탐 확인** (아래 §3 참조). close 처리 예정.

---

## 1. settings.json Hook 인벤토리

**등록된 Hook 이벤트 타입:**

| 이벤트 | 등록 hook 수 |
|--------|-------------|
| PreToolUse | 11개 (Write/Edit·Bash·Tavily·screenshot·chrome 매처) |
| PostToolUse | 22개 (*, Bash, Write/Edit, Read, tavily, stitch, TaskCreate/TodoWrite 등) |
| SubagentStop | 1개 |
| UserPromptSubmit | 5개 |
| PreCompact | 3개 (pre-compact-snapshot, audit-session, self-evolve) |
| Stop | 6개 |
| StopFailure | 1개 |
| PostCompact | 4개 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded / PostToolUseFailure | inline bash |
| SessionStart | 8개 |

---

## 2. Hook 파일 존재 검증

settings.json에 `$HOME/.claude/hooks/` 또는 `$HOME/.claude/scripts/`로 참조되는 모든 파일을 `harness/` 소스에서 확인.

| 분류 | 결과 |
|------|------|
| bash hooks (harness/hooks/*.sh) | ✅ 58개 전원 존재 |
| TypeScript hooks (user-prompt.ts, session-start.ts) | ✅ 2개 harness/hooks/ 존재 |
| scripts (audit-session.sh, self-evolve.sh, tavily-rotator.mjs) | ✅ harness/scripts/ 전원 존재 |

---

## 3. 경로 정확성 검증 — Issue #10 오탐 확인 (P-113 강제)

### 핵심 결론: settings.json은 이미 정상이었다

2026-07-02 감사 에이전트는 PreCompact `self-evolve` hook이 `hooks/`를 참조한다고 보고했으나, **이는 사실이 아님**.

git 이력 직접 검증:

```bash
git show c650362:harness/settings.json | grep -A5 PreCompact | grep self-evolve
# 결과: bash -c 'bash $HOME/.claude/scripts/self-evolve.sh --apply 2>/dev/null'
```

| 항목 | 내용 |
|------|------|
| **Issue #10 주장** | `bash $HOME/.claude/hooks/self-evolve.sh --apply 2>/dev/null` (hooks/ 참조) |
| **2026-05-23 c650362 실제 값** | `bash -c 'bash $HOME/.claude/scripts/self-evolve.sh --apply 2>/dev/null'` (scripts/ 참조) |
| **현재 settings.json 실제 값** | `bash -c 'bash $HOME/.claude/scripts/self-evolve.sh --apply 2>/dev/null'` (scripts/ 참조) |
| **git log 최근 수정** | settings.json 마지막 커밋 `e694761`·`c650362` (2026-05월) — 이후 변경 없음 |
| **판정** | ⚠️ Issue #10은 이전 감사 에이전트의 **오탐** (설정 파일 오독) |

**오탐 발생 원인 추정**: 이전 감사 에이전트가 별도의 경로(아카이브, 이전 버전, 또는 다른 설정 파일)를 읽었거나 문자열 생성 시 착오 발생.

**실제 현황**: `harness/scripts/self-evolve.sh` ✅ 존재 → deploy.sh: `~/.claude/scripts/` 배포 ✅ → settings.json: `scripts/` 참조 ✅ — **완전 정상**

---

## 4. 의존성 매트릭스 검증 (P-113 강제)

grep 직접 확인 — "같은 파일명 = 같은 path" 가정 금지. 전원 재검증.

| 소비자 hook | 기대 state 파일 | 공급원 | path 일치 |
|-------------|----------------|--------|-----------|
| `emergency-mode-check.sh` | `${HARNESS_STATE}/5h_usage.txt` (`HARNESS_STATE="${HOME}/.harness-state"` line 8) | `telegram-notify.sh` line 9: `STATE_DIR="$HOME/.harness-state"`, line 86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` | ✅ |
| `self-evolve-trigger.sh` | `$STATE_DIR/context_usage.txt` (`STATE_DIR="$HOME/.harness-state"` line 9) | `telegram-notify.sh` line 9: `STATE_DIR="$HOME/.harness-state"`, line 218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` | ✅ |
| `pitfall-auto-record-stop.sh` | `$HARNESS_STATE/pitfall_pending.json` (`HARNESS_STATE="${HOME}/.harness-state"` line 28) | `pitfall-auto-record.sh` line 25: `HARNESS_STATE="${HOME}/.harness-state"`, line 80: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | ✅ |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-{HASH}/build_detected` (`PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)`, line 14-15) | `build-detector.sh` line 37-42: 동일 `PROJECT_HASH` 산출식 + `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` + `DETECTED_FILE="$STATE_DIR/build_detected"` | ✅ |

→ **4/4 path 일치 확인** (2026-07-02 대비 변경 없음)

---

## 5. audit-session.sh check_ 함수 현황

| 항목 | 값 |
|------|---|
| 체크 수 | **67개** (이전 감사 대비 변경 없음) |
| 커버 버전 | v2.1.176 |
| 잔여 미커버 | v2.1.177 (changelog 미게재, 하네스 영향 신기능 없음) |

---

## 6. PITFALL 현황

- **총 파일 수**: **259개** (전일 대비 +1)
- **최신 번호**: P-272 (`expect-mcp-removed-verification-vs-navigation-misclassification`)
- **최근 7일 신규 (2026-06-27 ~ 2026-07-03)**:

| 번호 | 파일명 | 내용 |
|------|--------|------|
| P-272 | `expect-mcp-removed-verification-vs-navigation-misclassification` | expect MCP 제거 결정 기록 (2026-07-02 refactor 커밋, 대표님 승인 옵션 B) |

---

## 7. 부가 관찰 — dead code (non-blocking)

settings.json PreToolUse에 `mcp__expect__screenshot` 매처가 잔존:

```json
{
  "matcher": "mcp__expect__screenshot",
  "hooks": [{ "command": "bash $HOME/.claude/hooks/vision-routing-guard.sh" }]
}
```

- expect MCP는 2026-07-01 완전 제거 (`7b39486` 커밋)
- 매처가 발화할 수 없으므로 **silent fail 아님** — 단순 dead code
- 침묵 의심으로 분류하지 않음. 다음 settings.json 수정 시 정리 권장.

---

## 8. 결론

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 (58 sh + 2 ts) | ✅ 60개 전원 존재 |
| 경로 정확성 (settings.json ↔ 배포 경로) | ✅ 전원 정상 (Issue #10은 오탐) |
| 의존성 매트릭스 (P-113) | ✅ 4/4 pass |
| audit-session.sh 커버리지 | ✅ 67개 / v2.1.176 |
| **침묵 의심 총계** | ✅ **0건 CLEAR** |

→ **A) 분기 적용** — OPEN Issue #10 close + commit + push.

---

## 한계 명시

- **정적 분석만**. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈 확장**: git 이력 직접 조회(`git show <commit>:file | grep`)로 이전 감사 에이전트의 오탐을 검출. 단순 현재 파일 읽기만으로는 "경로가 원래 잘못되어 있었는지"를 판단 불가 — 이력 비교가 추가적 신뢰성 보장.
- **오탐 방지 권고**: 다음 감사부터 settings.json 특정 필드 읽기는 `jq` 또는 `python3 -c 'import json...'`로 구조화 파싱. 문자열 직접 grep은 따옴표/이스케이프로 오독 가능성 존재.
- v2.1.177: changelog 미게재 상태 지속. 다음 감사 시 재확인.
