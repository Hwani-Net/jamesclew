# JamesClaw 일일 자동 감사 — 2026-06-26

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-06-26  
**결과**: ⚠️ 침묵 의심 2건 — 2건 모두 **재발** (Issue #4 동일 항목, 11회차)

---

## 1. 사전 — 기존 OPEN Issue 조회

| No | Title | 생성일 | 상태 |
|----|-------|--------|------|
| **#4** | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 | OPEN |

→ 이번 감사 결과와 비교: **2건 모두 동일 재발** (11회차). Issue #4에 코멘트 추가.

---

## 2. settings.json Hook 인벤토리

**harness/settings.json 등록 hook 이벤트 타입 및 파일 존재 검증:**

| 이벤트 | 등록 hook 구성 | 파일 부재 |
|--------|--------------|----------|
| PreToolUse | Write\|Edit(3 cmds), mcp__tavily(1), mcp__expect__screenshot(1), chrome-in-chrome(1), Bash(추가 matchers) | 없음 |
| PostToolUse | 13개 matcher → 27개 명령 | 없음 |
| SubagentStop | `verify-subagent.sh` | 없음 |
| UserPromptSubmit | 5개 명령 | 없음 |
| PreCompact | `pre-compact-snapshot.sh`, `audit-session.sh --compact`, `self-evolve.sh --apply` | 없음 |
| Stop | 6개 명령 | 없음 |
| StopFailure | `telegram-notify.sh stop-failure` | 없음 |
| PostCompact | 4개 명령 | 없음 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | 각 1개 inline | 없음 |
| SessionStart | 8개 명령 | 없음 |
| PostToolUseFailure | `tool-duration-monitor.sh` | 없음 |

**파일 존재 검증 (harness/hooks/):**

| 항목 | 결과 |
|------|------|
| harness/hooks/ .sh 파일 (60개) | ✅ 전원 존재 |
| harness/scripts/audit-session.sh | ✅ 존재 |
| harness/scripts/self-evolve.sh | ✅ 존재 |

→ **파일 부재 침묵 의심 0건**

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 교훈 적용: writer의 STATE_DIR와 reader의 STATE_DIR를 grep으로 직접 비교.

| 소비자 hook | state 파일 | writer hook | reader path | writer path | 결과 |
|-------------|-----------|------------|------------|------------|------|
| `emergency-mode-check.sh` | `5h_usage.txt` | `telegram-notify.sh` | `${HARNESS_STATE}/5h_usage.txt` (L.9) | `$STATE_DIR/5h_usage.txt` (L.86) | ✅ |
| `self-evolve-trigger.sh` | `context_usage.txt` | `telegram-notify.sh` | `$STATE_DIR/context_usage.txt` (L.11) | `$STATE_DIR/context_usage.txt` (L.218) | ✅ |
| `pitfall-auto-record-stop.sh` | `pitfall_pending.json` | `pitfall-auto-record.sh` | `$HARNESS_STATE/pitfall_pending.json` (L.29) | `$HARNESS_STATE/pitfall_pending.json` (L.80) | ✅ |
| `enforce-build-transition.sh` | `build-{HASH}/build_detected` | `build-detector.sh` | `$HOME/.harness-state/build-$(echo $PWD\|md5sum\|cut -c1-8)/` (L.14-15) | 동일 계산식 (L.37-38) | ✅ |

→ **의존성 매트릭스 4/4 ✅ CLEAR**

---

## 4. audit-session.sh 검증

| 항목 | 결과 |
|------|------|
| check_ 함수 개수 | **56개** |
| 마지막 커버 버전 | **v2.1.144** (`check_v144_mcp_paginated_tools`) |
| 마지막 업데이트 | **2026-05-19** |
| 현재 Claude Code 버전 (CLAUDE.md 기준) | **v2.1.177** |
| 감사 공백 버전 범위 | **v2.1.145 ~ v2.1.177 (33개 버전)** |

### ⚠️ 침묵 의심 1: check_v144_model_single_session WARN 메시지 오류 (재발 11회차)

**파일**: `harness/scripts/audit-session.sh` L.1019 (직접 확인)  
**현황**: WARN 메시지에 v2.1.153에서 제거된 `d` 키 참조가 여전히 존재.

```bash
# 현재 (잘못됨, L.1019):
"WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```

**수정 방향** (CLAUDE.md v2.1.153 정책 반영):
```bash
"WARN|settings.json model 필드 미명시 — v2.1.153+ /model 영구화(원복). opusplan 고정 시 settings.json model 필드 또는 /model 1회 호출 후 자동 저장. 현재세션만 변경 시 picker 's' 키 사용. ('d' 키는 v2.1.153 제거됨)"
```

### ⚠️ 침묵 의심 2: v2.1.145~v2.1.177 check 함수 0개 (재발 11회차)

33개 버전의 하네스 영향 신기능이 감사에서 누락됨. 어제(v2.1.173 기준) 대비 4개 버전 추가 누락(v2.1.174~v2.1.177).

**추가 필요 check 함수 (CLAUDE.md v2.1.177 기준):**

| 버전 | 변경 | 필요 check 함수 |
|------|------|----------------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8(default high effort) + `/effort xhigh` + Dynamic Workflows(`/workflows`) | `check_v154_opus48_effort`, `check_v154_dynamic_workflows` |
| v2.1.159 | 트리거어 `workflow`→`ultracode` | `check_v159_ultracode_trigger` |
| v2.1.163 | Stop/SubagentStop hook `additionalContext` + Bash if 오발동 수정 | `check_v163_hook_additional_context` |
| v2.1.166 | `fallbackModel` + `MAX_THINKING_TOKENS=0` | `check_v166_fallback_model` |
| v2.1.169 | `--safe-mode` + `/cd` + `disableBundledSkills` | `check_v169_safe_mode` |
| v2.1.170 | Claude Fable 5 출시 (⛔ 2026-06-21 STICKY 금지) | `check_v170_fable5_banned` |
| v2.1.172 | 서브에이전트 중첩 스폰(최대 5단계) + 1M auto compact 복구 | `check_v172_nested_subagent` |
| v2.1.173 | Fable 5 `[1m]` suffix 자동 strip | `check_v173_fable5_suffix_strip` |
| v2.1.174 | Workflow `agent()` attribution + background env var 상속 수정 | `check_v174_workflow_attribution` |
| v2.1.175 | `enforceAvailableModels` | `check_v175_enforce_models` |
| v2.1.176 | 세션 제목 대화 언어 생성 + hook `if` 경로 매칭 수정 + auto mode Fable 5 폴백 | `check_v176_hook_if_path_fix` |
| v2.1.177 | changelog 미게재 — 검토 필요 | 검토 후 추가 |

**[어제 대비 신규]** v2.1.174~v2.1.177 — 4개 버전 추가 누락. hook `if` 경로 매칭 수정(v2.1.176)은 `verify-deploy.sh`, `quality-gate.sh`, `enforce-review.sh` 직접 영향 가능.

---

## 5. PITFALL 개수 + 최근 7일 신규 항목

| 항목 | 결과 |
|------|------|
| 전체 pitfall 파일 수 | **258개** |
| 최고 번호 | **P-271** (`pitfall-271-sandbox-mount-desync-from-authoritative-file.md`) |
| 최근 7일 신규 pitfall (git log --since=2026-06-19) | **3건** |

**최근 7일 신규 pitfall:**

| No | 슬러그 | 커밋 |
|----|--------|------|
| P-269 | `cowork-desktop-mcp-separate-from-cli-codex-mcp-server` | `4cfd07a` (docs: sync manual to v2.1.177) |
| P-270 | `fable5-banned-broad-rename-collateral` | `2bf68df` (chore: ban Fable 5 → Opus 4.8) |
| P-271 | `sandbox-mount-desync-from-authoritative-file` | `6b311f1` (feat: codebase-memory-mcp + durable-exec) |

---

## 6. 결과 분기: B) 침묵 의심 2건 (재발)

- Issue #4 항목 동일 → 기존 Issue에 코멘트 추가 (신규 Issue 미생성)
- 보고서 commit + push

---

## 한계 명시

- 정적 분석만. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- PITFALL 최근 7일 신규 항목: git log (--since=2026-06-19) 정적 확인 — 3건 (P-269/P-270/P-271).
