# 일일 자동 감사 보고서 — 2026-07-16

**감사 일시**: 2026-07-16 KST 09:00 (UTC 00:00)  
**에이전트**: harness-audit-daily (정적 분석 전용)  
**결과**: ⚠️ 침묵 의심 2건 (재발)

---

## 1. Hook 파일 인벤토리

settings.json 등록 hook 파일 58개 전원 `harness/hooks/` 에 실제 존재.  
**누락: 0건 ✅**

주요 확인 목록:
- PreToolUse: verify-memory-write.sh, enforce-build-transition.sh, tavily-guardrail.sh, vision-routing-guard.sh, chrome-read-page-guard.sh, cdp-auto-ensure.sh, quality-gate.sh, pre-commit-conventional.sh, irreversible-alert.sh, bash-tool-blocker.sh, verify-deploy.sh
- PostToolUse: periodic-audit.sh, agentmemory-mirror-obsidian.sh, cdp-mark-fail.sh, enforce-review.sh, error-telegram.sh, loop-detector.sh, timeline-logger.sh, post-edit-dispatcher.sh, regression-autotest.sh, enforce-cross-review.sh, change-tracker.sh, regression-guard.sh, test-manipulation-guard.sh, read-once.sh, sonnet-vision-delegate-guard.sh, explore-router.sh, log-filter.sh, cost-tracker.sh, wiki-raw-save.sh, progressive-summarize.sh, stitch-drift-guard.sh, watchdog-ralph.sh, task-queue-sort.sh, tool-duration-monitor.sh
- UserPromptSubmit: user-prompt.ts, user-prompt-declare-warn.sh, capture-reset-times.sh, pitfall-auto-record.sh, build-detector.sh
- SubagentStop: verify-subagent.sh
- PreCompact: pre-compact-snapshot.sh
- Stop: stop-dispatcher.sh, session-learning.sh, emergency-mode-check.sh, inbox-classifier.sh, self-evolve-trigger.sh, pitfall-auto-record-stop.sh
- StopFailure: telegram-notify.sh
- PostCompact: telegram-notify.sh, session-start.ts, post-compact-resume.sh, dialectic-pattern-extractor.sh
- SessionStart: telegram-notify.sh, emergency-banner.sh, session-start-active-infra.sh, codex-expiry-check.sh, session-start.ts, wiki-sync-scheduler.sh, harness-self-audit.sh, distress-queue-loader.sh

---

## 2. 의존성 매트릭스 검증 (P-111 + P-113)

**P-113 강제 — writer/reader STATE_DIR + PROJECT_HASH 직접 grep 확인.**

| 소비자 hook | state 파일 | 공급원 | writer PATH | reader PATH | 일치 |
|------------|-----------|-------|------------|------------|------|
| emergency-mode-check.sh | `~/.harness-state/5h_usage.txt` | telegram-notify.sh L86 | `$STATE_DIR/5h_usage.txt` (`STATE_DIR=$HOME/.harness-state`) | `$HARNESS_STATE/5h_usage.txt` (`HARNESS_STATE=$HOME/.harness-state`) | ✅ |
| self-evolve-trigger.sh | `~/.harness-state/context_usage.txt` | telegram-notify.sh L218 | `$STATE_DIR/context_usage.txt` (`STATE_DIR=$HOME/.harness-state`) | `$STATE_DIR/context_usage.txt` (`STATE_DIR=$HOME/.harness-state`) | ✅ |
| pitfall-auto-record-stop.sh | `~/.harness-state/pitfall_pending.json` | pitfall-auto-record.sh L80 | `$HARNESS_STATE/pitfall_pending.json` (`$HOME/.harness-state`) | `$HARNESS_STATE/pitfall_pending.json` (`$HOME/.harness-state`) | ✅ |
| enforce-build-transition.sh | `~/.harness-state/build-{HASH}/build_detected` | build-detector.sh L42 | `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` → `$STATE_DIR/build_detected` | 동일 md5sum 로직, 동일 경로 패턴 | ✅ |

4개 의존성 체인 모두 PATH 일치 확인. **침묵 의심 없음.**

---

## 3. audit-session.sh check_ 함수 개수

- **현재**: 67개 (기준 39개, 2026-04-29 기준)
- 마지막 추가 함수: `check_v176_hook_if_path_fix`, `check_v119_config_persistence`, `check_design`
- v2.1.177까지의 신기능 중 감사 미반영 여부: **별도 항목 4번에서 침묵 의심 발견됨**

---

## 4. ⚠️ 침묵 의심 — 2건 (재발)

### 4-1. `check_v170_fable5_banned` — 재발 (Issue #11 미수정)

**위치**: `harness/scripts/audit-session.sh` L1154–1165

**현재 코드 (변화 없음)**:
```bash
check_v170_fable5_banned() {
  local settings_file="$HOME/.claude/settings.json"
  if [ -f "$settings_file" ]; then
    if grep -qi '"model".*fable\|fable.*"model"' "$settings_file" 2>/dev/null; then
      echo "FAIL|settings.json model에 Fable 5 감지 — STICKY DECISIONS 위반! 2026-06-21 대표님 금지 지시. 즉시 claude-opus-4-8로 교체 필요"
    else
      echo "PASS|Fable 5 미사용 — v2.1.170 STICKY 금지 준수 (재도입 금지)"
    fi
  else
    echo "PASS|settings.json 없음이나 Fable 5 미명시 — STICKY 금지 위반 없음"
  fi
}
```

**현황**: 2026-07-03 Fable 5 금지 해제·재도입 (대표님 STICKY, CLAUDE.md 폐기 표) 이후 코드가 전혀 수정되지 않음. settings.json에 `fallbackModel: ["claude-sonnet-4-6"]` 등 Fable 5 관련 설정이 없어 현재는 우연히 PASS를 내고 있지만, PASS 메시지 문구 "재도입 금지"는 정책과 정반대. FAIL 경로는 오경보 유발 잠재성 유지.

**Issue #11** (2026-07-06 생성): 미수정 재발.

---

### 4-2. `check_v173_fable5_suffix_strip` — 재발 (Issue #12 미수정)

**위치**: `harness/scripts/audit-session.sh` L1175–1182

**현재 코드 (변화 없음)**:
```bash
check_v173_fable5_suffix_strip() {
  local claude_md="$HOME/.claude/CLAUDE.md"
  if [ -f "$claude_md" ] && grep -q "Fable.*금지\|fable.*banned\|claude-fable-5.*금지" "$claude_md" 2>/dev/null; then
    echo "PASS|CLAUDE.md Fable 5 금지 정책 확인 — v2.1.173 [1m] suffix strip 무관 (STICKY 금지 유지)"
  else
    echo "WARN|CLAUDE.md Fable 5 금지 정책 미확인 — v2.1.173 [1m] suffix strip 인지 필요"
  fi
}
```

**현황**: CLAUDE.md 폐기 표에 "~~Claude Fable 5~~ → 2026-07-03 금지 해제" 텍스트가 포함되어 있어, `grep -q "Fable.*금지"` 패턴이 해당 행에 매칭 → PASS 출력. 하지만 PASS 메시지는 "STICKY 금지 유지"로 정책과 정반대. 2026-07-03 재도입 결정이 미반영된 상태가 지속.

**Issue #12** (2026-07-09 생성): 미수정 재발.

---

## 5. PITFALL 현황

- **총 PITFALL 수**: 259개
- **최신 파일**: pitfall-272-expect-mcp-removed-verification-vs-navigation-misclassification.md (2026-07-09)
- **최근 7일 신규 (2026-07-10 ~ 2026-07-16)**: 0건

---

## 6. 조치

- 침묵 의심 2건 모두 이전 OPEN Issue 항목 재발 → Issue #11, #12에 재발 코멘트 추가
- 보고서 commit + push

---

## 한계 명시

- **정적 분석만**. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성을 사전 감지하는 보완 레이어.
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 4개 의존성 체인 직접 grep 확인 완료.
- check_ 함수 67개는 v2.1.170~v2.1.177 기능 일부 반영. 향후 신규 버전 기능 추가 시 `audit-session.sh` 동기화 필요.
