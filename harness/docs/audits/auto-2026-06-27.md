# JamesClaw 일일 자동 감사 — 2026-06-27

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-06-27  
**결과**: ⚠️ 침묵 의심 2건 — 2건 모두 **재발** (Issue #4 동일 항목, 12회차)

---

## 1. 사전 — 기존 OPEN Issue 조회

| No | Title | 생성일 | 최종 업데이트 | 코멘트 수 |
|----|-------|--------|-------------|----------|
| **#4** | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 | 2026-06-26 | 25 |

→ 이번 감사 결과와 비교: **2건 모두 동일 재발** (12회차). Issue #4에 코멘트 추가.

---

## 2. settings.json Hook 인벤토리 검증

### 등록된 Hook 이벤트 타입

| 이벤트 | Hook 파일 수 | 상태 |
|--------|------------|------|
| PreToolUse | 11개 (verify-memory-write, enforce-build-transition, tavily-guardrail, vision-routing-guard, chrome-read-page-guard, cdp-auto-ensure, quality-gate, pre-commit-conventional, irreversible-alert, bash-tool-blocker, verify-deploy) | ✅ |
| PostToolUse | 20개 (periodic-audit, agentmemory-mirror-obsidian, cdp-mark-fail, verify-deploy, enforce-review, error-telegram, loop-detector, timeline-logger, post-edit-dispatcher, regression-autotest, enforce-cross-review, change-tracker, regression-guard, test-manipulation-guard, read-once, sonnet-vision-delegate-guard, explore-router, log-filter, cost-tracker, wiki-raw-save, progressive-summarize, stitch-drift-guard, quality-gate, watchdog-ralph, task-queue-sort, tool-duration-monitor) | ✅ |
| UserPromptSubmit | 5개 (user-prompt.ts, user-prompt-declare-warn, capture-reset-times, pitfall-auto-record, build-detector) | ✅ |
| PreCompact | 3개 (pre-compact-snapshot, audit-session.sh, self-evolve.sh) | ✅ |
| Stop | 6개 (stop-dispatcher, session-learning, emergency-mode-check, inbox-classifier, self-evolve-trigger, pitfall-auto-record-stop) | ✅ |
| StopFailure | 1개 (telegram-notify.sh stop-failure) | ✅ |
| PostCompact | 4개 (telegram-notify.sh compact, session-start.ts, post-compact-resume, dialectic-pattern-extractor) | ✅ |
| SubagentStop | 1개 (verify-subagent) | ✅ |
| SessionStart | 8개 (telegram-notify.sh start inline, emergency-banner, session-start-active-infra, codex-expiry-check, session-start.ts, wiki-sync-scheduler, harness-self-audit, distress-queue-loader) | ✅ |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | inline bash (파일 없음) | ✅ 정상 |
| PostToolUseFailure | 1개 (tool-duration-monitor) | ✅ |

**결론: 모든 hook이 harness/hooks/ (또는 scripts/)에 실제 존재. 파일 부재 0건.**

---

## 3. harness/hooks/ 실제 파일 vs settings.json 비교

**파일 부재 의심: 0건** ✅  
(ConfigChange/WorktreeCreate/WorktreeRemove/InstructionsLoaded는 inline bash로 파일 불필요)

---

## 4. 의존성 매트릭스 검증 (P-111 + P-113)

| 소비자 hook | 기대 state 파일 | 소비자 정의 경로 | 공급자 경로 | 일치 |
|-------------|---------------|---------------|-----------|------|
| emergency-mode-check.sh | `~/.harness-state/5h_usage.txt` | `HARNESS_STATE="${HOME}/.harness-state"` + `USAGE_FILE="${HARNESS_STATE}/5h_usage.txt"` | telegram-notify.sh: `STATE_DIR="$HOME/.harness-state"` + `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` | ✅ |
| self-evolve-trigger.sh | `~/.harness-state/context_usage.txt` | `STATE_DIR="$HOME/.harness-state"` + `CONTEXT_FILE="$STATE_DIR/context_usage.txt"` | telegram-notify.sh: `STATE_DIR="$HOME/.harness-state"` + `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` | ✅ |
| pitfall-auto-record-stop.sh | `~/.harness-state/pitfall_pending.json` | `HARNESS_STATE="${HOME}/.harness-state"` + `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | pitfall-auto-record.sh: `HARNESS_STATE="${HOME}/.harness-state"` + `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | ✅ |
| enforce-build-transition.sh | `~/.harness-state/build-{HASH}/build_detected` | `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` + `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` | build-detector.sh: 동일 `PROJECT_HASH` + `STATE_DIR` 정의, `DETECTED_FILE="$STATE_DIR/build_detected"` | ✅ |

**P-113 강제 결과: 4/4 경로 일치 ✅ (PROJECT_HASH 사용 여부 포함)**

---

## 5. audit-session.sh check_ 함수 분석

| 항목 | 현황 |
|------|------|
| 현재 check_ 함수 수 | **56개** |
| 마지막 커버 버전 | **v2.1.144** |
| 현재 Claude Code 버전 (CLAUDE.md 기준) | **v2.1.177** |
| 감사 공백 버전 수 | **33개 버전 (v2.1.145~v2.1.177)** |
| audit-session.sh 최근 수정 | 없음 (2026-06-20 이후 git 변경 없음) |

### ❌ 침묵 의심 1: check_v144_model_single_session WARN 메시지 폐기된 `d` 키 참조

**위치**: `harness/scripts/audit-session.sh` line ~1020  
**현재 (잘못됨)**:
```
"WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```
**사유**: `d` 액션은 v2.1.153에서 제거됨 (CLAUDE.md 명시). 현재 v2.1.177 기준 `s` 키(현재세션)만 존재. 오류 가이던스 출력 지속.

### ❌ 침묵 의심 2: v2.1.145~v2.1.177 check 함수 0개

**감사 공백 주요 버전별 누락 check 함수**:

| 버전 | 변경 내용 | 필요 check 함수 |
|------|---------|----------------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8 + `/effort xhigh` + Dynamic Workflows (`/workflows`) | `check_v154_opus48_effort`, `check_v154_dynamic_workflows` |
| v2.1.159 | 트리거어 `workflow`→`ultracode` | `check_v159_ultracode_trigger` |
| v2.1.163 | Stop/SubagentStop hook `additionalContext` + hook `if:"Bash()"` fix | `check_v163_hook_additional_context` |
| v2.1.166 | `fallbackModel` + cross-session `SendMessage` 권한 + `MAX_THINKING_TOKENS=0` | `check_v166_fallback_model`, `check_v166_send_message_user_perm` |
| v2.1.169 | `--safe-mode`, `/cd`, `disableBundledSkills` | `check_v169_safe_mode`, `check_v169_cd_command` |
| v2.1.170 | Claude Fable 5 출시 (우리: **사용 금지** STICKY) | `check_v170_fable5_banned` |
| v2.1.172 | 서브에이전트 중첩 최대 5단계 + 1M compact 복구 + `opusplan[1m]` | `check_v172_subagent_nesting` |
| v2.1.176 | 세션 제목 한국어 생성 + hook `if` Read/Edit/Write path 수정 | `check_v176_session_title_lang` |

---

## 6. PITFALL 현황

| 항목 | 값 |
|------|---|
| 전체 PITFALL 파일 수 | **258개** |
| 최고 번호 | **P-271** |

### 최근 7일 신규 PITFALL (2026-06-20 이후)

| PITFALL | 제목 | 추가 커밋 |
|---------|------|---------|
| P-259 | fable5-adoption-harness-diet-stale-model-anchors | `6b311f1` |
| P-262 | codex-oauth-token-expired (재발 기록 추가) | `07252cc` |
| P-269 | cowork-desktop-mcp-separate-from-cli-codex-mcp-server | `4cfd07a` |
| P-270 | fable5-banned-broad-rename-collateral | `2bf68df` |
| P-271 | sandbox-mount-desync-from-authoritative-file | `6b311f1` |

---

## 7. 결과 분기: B) 침묵 의심 2건 — 재발 (12회차)

### 재발 판정 근거

| 의심 항목 | Issue #4 항목 일치 | 상태 |
|-----------|-----------------|------|
| check_v144 WARN `d` 키 오류 | ✅ 동일 | 미수정 지속 |
| v2.1.145~현재 check 함수 공백 | ✅ 동일 (공백 범위 확대: 158→177) | 미수정 지속 |

→ **신규 Issue 생성 없음. Issue #4에 재발 코멘트 추가.**

---

## 한계 명시

- 정적 분석만. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- audit-session.sh 미수정 12연속: 자동 감사 에이전트는 발견·보고만 수행. 수정은 메인 세션 자율 결정 또는 대표님 지시 필요.
