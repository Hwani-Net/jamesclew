# 일일 자동 감사 — 2026-07-07

**감사 일시**: 2026-07-07 KST 09:00 (자동 감사 에이전트)  
**결과**: ⚠️ 침묵 의심 2건 (재발 1건 + 신규 1건)

---

## 1. Hook 인벤토리 검증

settings.json에 등록된 전체 hook 파일을 harness/hooks/에서 대조 검증.

| 이벤트 타입 | 등록 hook 수 | 파일 존재 |
|------------|------------|----------|
| PreToolUse | 11개 | ✅ 전체 |
| PostToolUse | 19개 | ✅ 전체 |
| UserPromptSubmit | 5개 | ✅ 전체 |
| Stop | 6개 | ✅ 전체 |
| SessionStart | 8개 | ✅ 전체 |
| StopFailure | 1개 | ✅ 전체 |
| PostCompact | 4개 | ✅ 전체 |
| PreCompact | 3개 | ✅ 전체 |
| SubagentStop | 1개 | ✅ 전체 |
| PostToolUseFailure | 1개 | ✅ 전체 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | inline | — |

**결론**: 파일 부재로 인한 침묵 의심 0건.

---

## 2. 의존성 매트릭스 검증 (P-113)

### 2-1. emergency-mode-check.sh ↔ telegram-notify.sh
- **reader**: `HARNESS_STATE="${HOME}/.harness-state"` → `USAGE_FILE="${HARNESS_STATE}/5h_usage.txt"`
- **writer**: `STATE_DIR="$HOME/.harness-state"` → `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"`
- **판정**: ✅ PATH 일치 (`~/.harness-state/5h_usage.txt`)

### 2-2. self-evolve-trigger.sh ↔ telegram-notify.sh
- **reader**: `STATE_DIR="$HOME/.harness-state"` → `CONTEXT_FILE="$STATE_DIR/context_usage.txt"`
- **writer**: `STATE_DIR="$HOME/.harness-state"` → `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"`
- **판정**: ✅ PATH 일치 (`~/.harness-state/context_usage.txt`)

### 2-3. pitfall-auto-record-stop.sh ↔ pitfall-auto-record.sh
- **reader**: `HARNESS_STATE="${HOME}/.harness-state"` → `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"`
- **writer**: `HARNESS_STATE="${HOME}/.harness-state"` → `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"`
- **판정**: ✅ PATH 일치 (`~/.harness-state/pitfall_pending.json`)

### 2-4. enforce-build-transition.sh ↔ build-detector.sh
- **reader**: `PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)` → `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` → `$STATE_DIR/build_detected`
- **writer**: `PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)` → `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` → `$STATE_DIR/build_detected`
- **판정**: ✅ PATH 일치 (PROJECT_HASH 공식 동일, `~/.harness-state/build-{HASH}/build_detected`)

---

## 3. audit-session.sh check_ 함수 현황

- **현재 count**: **67개** (grep `^check_` 실측)
- 최신 check 함수: `check_v176_hook_if_path_fix`, `check_v119_config_persistence`, `check_design`
- Claude Code 최신 버전 반영: v2.1.176까지 check 존재 ✅

---

## 4. ⚠️ 침묵 의심 — 2건

### [침묵 의심 1] `check_v170_fable5_banned` 로직 스탈 — **재발** (Issue #11 동일)

**위치**: `harness/scripts/audit-session.sh` L1154  
**함수**: `check_v170_fable5_banned()`

```bash
# 현재 로직 (스탈)
if grep -qi '"model".*fable\|fable.*"model"' "$settings_file"; then
  echo "FAIL|Fable 5 감지 — STICKY DECISIONS 위반! 2026-06-21 대표님 금지 지시."
else
  echo "PASS|Fable 5 미사용 — v2.1.170 STICKY 금지 준수 (재도입 금지)"
fi
```

**실제 STICKY DECISIONS** (CLAUDE.md 2026-07-03 확인):
- Fable 5 **금지 해제** — 2026-07-03 대표님 명시 재도입
- 메인 세션 모델 = `claude-fable-5` (폴백: `claude-opus-4-8`)
- 폐기 표: `~~2026-06-21~~ → **2026-07-03 금지 해제**` 명시

**영향**: Fable 5가 settings.json에 올바르게 설정되어 있어도 `FAIL` 발생 → 잘못된 롤백 유도

---

### [침묵 의심 2] `check_v173_fable5_suffix_strip` 오해 소지 메시지 — **신규**

**위치**: `harness/scripts/audit-session.sh` L1175  
**함수**: `check_v173_fable5_suffix_strip()`

```bash
# 현재 로직
if grep -q "Fable.*금지\|fable.*banned\|claude-fable-5.*금지" "$claude_md"; then
  echo "PASS|CLAUDE.md Fable 5 금지 정책 확인 — v2.1.173 [1m] suffix strip 무관 (STICKY 금지 유지)"
```

**문제**: CLAUDE.md는 `"Fable 5 금지 해제"` 등 과거 이력 텍스트를 포함하고 있어 grep에 걸림.  
→ PASS 출력 시 메시지가 `"STICKY 금지 유지"` → **금지 해제 결정과 역전된 메시지 출력**  
→ Fable 5가 메인 모델인 상황에서 `suffix strip 무관` 주석도 오해 소지 (실제로는 관련 있음)

**근본 원인**: Issue #11의 check_v170 수정과 함께 check_v173도 동시 수정 필요했으나 누락.

---

## 5. PITFALL 현황

- **총 PITFALL 파일 수**: 259개
- **최근 7일 신규** (2026-07-01 이후):
  - `pitfall-272-expect-mcp-removed-verification-vs-navigation-misclassification.md` (expect MCP 완전 제거 후 검증 vs 네비게이션 도구 분류 혼동)

---

## 6. 재발 분석

| 침묵 의심 항목 | 이전 Issue | 판정 |
|--------------|-----------|------|
| check_v170_fable5_banned | #11 (2026-07-06 OPEN) | **재발** — fix 미적용 |
| check_v173_fable5_suffix_strip | 없음 | 신규 (check_v170 fix 시 함께 처리 필요) |

---

## 7. 권장 수정

```bash
# harness/scripts/audit-session.sh

# Check 63 수정: check_v170_fable5_reinstated
check_v170_fable5_reinstated() {
  # 2026-07-03 대표님 금지 해제 — Fable 5 메인 모델 재도입
  local settings_file="$HOME/.claude/settings.json"
  if [ -f "$settings_file" ]; then
    if grep -qi 'claude-fable-5\|"model".*fable\|fable.*"model"' "$settings_file" 2>/dev/null; then
      echo "PASS|Fable 5 메인 모델 확인 — 2026-07-03 대표님 재도입 STICKY 준수"
    else
      echo "INFO|Fable 5 미명시 — /model claude-fable-5 또는 settings.json model 필드 확인 권고 (2026-07-03 STICKY)"
    fi
  else
    echo "INFO|settings.json 없음 — 런타임 /model 선택으로 관리되는 환경"
  fi
}

# Check 66 수정: check_v173_fable5_suffix_strip
check_v173_fable5_suffix_strip() {
  # 2026-07-03 금지 해제 이후 Fable 5는 메인 모델 — suffix strip은 실제 관련 있음
  local claude_md="$HOME/.claude/CLAUDE.md"
  if [ -f "$claude_md" ] && grep -q "2026-07-03.*금지 해제\|금지 해제.*fable\|fable.*금지 해제" "$claude_md" 2>/dev/null; then
    echo "PASS|CLAUDE.md Fable 5 금지 해제 확인 — v2.1.173 [1m] suffix strip 적용 (claude-fable-5 = claude-fable-5[1m])"
  else
    echo "WARN|CLAUDE.md Fable 5 금지 해제 미확인 — 2026-07-03 STICKY 결정 반영 여부 점검 필요"
  fi
}
```

---

## 8. 한계 명시

- **정적 분석 전용**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당
- **본 에이전트 역할**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어
- **P-113 교훈 준수**: "기능 정상" 결론 시 path 일치까지 검증 (완료 ✅)
- **check_ 함수 카운트 기준**: 이 보고서 감사 시점 기준 67개. 신규 기능 추가 시 check_함수 동시 추가 필요 (CLAUDE.md §"감사 항목 동기화 필수")
