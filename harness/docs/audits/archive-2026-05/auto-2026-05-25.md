# 일일 자동 감사 — 2026-05-25

**실행 시각**: 2026-05-25 KST 09:00 (UTC 00:00)  
**결과**: ✅ **침묵 의심 0건 CLEAR**  
**기존 OPEN Issue (harness-audit)**: 0건

---

## 1. 기존 OPEN Issue 조회

`mcp__github__list_issues` 조회 결과 `harness-audit` 라벨 OPEN Issue **0건**.

---

## 2. Hook 인벤토리 검증

settings.json 파싱 결과 등록된 hook 타입:

| 이벤트 | 등록 hook 수 |
|--------|------------|
| PreToolUse | 11개 |
| PostToolUse | 20개 |
| UserPromptSubmit | 5개 |
| SubagentStop | 1개 |
| PreCompact | 3개 |
| Stop | 6개 |
| StopFailure | 1개 |
| PostCompact | 4개 |
| SessionStart | 8개 |
| PostToolUseFailure | 1개 |
| ConfigChange | 1개 |
| WorktreeCreate | 1개 |
| WorktreeRemove | 1개 |
| InstructionsLoaded | 1개 |

**harness/hooks/ 실제 존재 여부**: 모든 등록 hook 파일 확인 ✅

미등록 파일(hooks/에는 존재하나 settings.json에 미등록):
- `awesome-statusline.sh` — statusline 전용, 별도 경로 등록
- `curation.ts` — 실험적 파일
- `enforce-execution.sh`, `evidence-first.sh` — stop-dispatcher.sh 등 내부 호출 방식
- `test-pre-commit-conventional.sh` — 테스트용 스크립트

→ 기능 침묵 의심 없음 ✅

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제 — writer/reader 모두 실제 코드에서 path 일치 grep 확인.

| 소비자 hook | 기대 state 파일 | 공급원 | 검증 결과 |
|-------------|---------------|--------|----------|
| `emergency-mode-check.sh` | `${HOME}/.harness-state/5h_usage.txt` | `telegram-notify.sh` → `$STATE_DIR/5h_usage.txt` (STATE_DIR=`$HOME/.harness-state`) | ✅ 일치 |
| `self-evolve-trigger.sh` | `$STATE_DIR/context_usage.txt` (STATE_DIR=`$HOME/.harness-state`) | `telegram-notify.sh` → `$STATE_DIR/context_usage.txt` | ✅ 일치 |
| `pitfall-auto-record-stop.sh` | `$HARNESS_STATE/pitfall_pending.json` (`$HOME/.harness-state`) | `pitfall-auto-record.sh` → `$HARNESS_STATE/pitfall_pending.json` | ✅ 일치 |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-$PROJECT_HASH/build_detected` (PROJECT_HASH=`echo "$PWD" \| md5sum \| cut -c1-8`) | `build-detector.sh` → 동일 PROJECT_HASH 공식 + 동일 STATE_DIR | ✅ 일치 |

**4/4 path 일치 — path mismatch 의심 없음** ✅

---

## 4. audit-session.sh check_ 함수 개수

| 항목 | 값 |
|------|-----|
| 총 check_ 함수 수 | **56개** |
| 이전 기준 (2026-04-29) | 39개 |
| 신규 추가 | +17개 |
| 최신 커버리지 | v2.1.144 (`check_v144_model_single_session`, `check_v144_mcp_paginated_tools`) |

CLAUDE.md 등재 최신 버전 **v2.1.144**까지 audit 커버리지 완전 ✅

---

## 5. PITFALL 현황

**총 PITFALL 파일 수**: 170개 (최고 번호 P-173, 일부 번호 중복 슬러그 포함)

### 최근 7일 신규 항목 (2026-05-18 ~ 2026-05-25)

| PITFALL | 제목 | 커밋 |
|---------|------|------|
| P-173 | settings.json template invalid escape 자동 revert | `0a8dc9f` |
| P-172 | 세션 인수인계 실패 — gbrain 폐기 결정 부활 | `867b1d9` |
| P-171 | gbrain config set 시크릿 누출 | `90e2447` |
| P-135 | Sonnet Vision 대칭성 맹점 | `974b239` |
| P-134 | LivePortrait 오디오 미지원 | `974b239` |
| P-133 | SadTalker 애니메이션 비대칭 | `974b239` |
| P-132 | 캐릭터 정적·과용·자막 트리플 | `d34897c` |

**7일 신규**: 7건

---

## 6. 부가 관찰 사항

### auto-2026-05-21.md 감사 누락 (전주 발견, 지속 관찰)
`auto-2026-05-21.md` 파일 부재 — 로컬 PC 스케줄러(Windows Task Scheduler) 미실행 추정. 정적 감사 범위 외.

---

## 결론 및 한계 명시

**침묵 의심 0건 CLEAR** — 신규 Issue 미생성, 텔레그램 미발송.

### 한계
- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당
- **본 에이전트**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 본 감사에서 이행 ✅
- **D:/jamesclew/ 하드코딩 경로**: 원격 환경(Linux)에서 실제 실행 불가. 런타임 검증은 로컬 PC 담당
