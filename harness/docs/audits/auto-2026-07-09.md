# JamesClew Harness 일일 자동 감사 — 2026-07-09

**감사 일시**: 2026-07-09 KST 09:00 (UTC 00:00)  
**에이전트**: harness-audit-daily (원격 자동 실행)  
**판정**: ⚠️ **침묵 의심 2건** — 재발 1 / 신규 1

---

## 1. Hook 인벤토리 검증

**settings.json 등록 hook 이벤트 타입**: PreToolUse · PostToolUse · SubagentStop · UserPromptSubmit · PreCompact · Stop · StopFailure · PostCompact · ConfigChange · WorktreeCreate · WorktreeRemove · InstructionsLoaded · SessionStart · PostToolUseFailure

**고유 hook 파일 수**: 58개 (`.sh` 56 + `.ts` 2)

| 결과 | 수 |
|------|----|
| ✅ 파일 존재 | 58 |
| ❌ 파일 부재 (침묵 1순위) | 0 |

**판정: ALL PRESENT** ✅

---

## 2. 의존성 매트릭스 검증 (P-111 + P-113)

| 소비자 hook | 기대 state 파일 | 공급원 | STATE_DIR 일치 | PROJECT_HASH 일치 |
|------------|----------------|--------|---------------|------------------|
| emergency-mode-check.sh | `~/.harness-state/5h_usage.txt` | telegram-notify.sh L86 | ✅ `${HOME}/.harness-state` = `$HOME/.harness-state` | N/A |
| self-evolve-trigger.sh | `~/.harness-state/context_usage.txt` | telegram-notify.sh L218 | ✅ `$HOME/.harness-state` 양쪽 일치 | N/A |
| pitfall-auto-record-stop.sh | `~/.harness-state/pitfall_pending.json` | pitfall-auto-record.sh L80 | ✅ `${HOME}/.harness-state` 양쪽 일치 | N/A |
| enforce-build-transition.sh | `~/.harness-state/build-{HASH}/build_detected` | build-detector.sh L38-42 | ✅ `$HOME/.harness-state/build-$PROJECT_HASH` 양쪽 일치 | ✅ `md5sum \| cut -c1-8` 동일 공식 |

**판정: 의존성 매트릭스 4/4 경로 일치** ✅

---

## 3. audit-session.sh check_ 함수 분석

- **현재 check_ 함수 수**: **67개** (2026-04-29 기준 39개 → +28 추가됨)
- **최신 커버 버전**: v2.1.176 (`check_v176_hook_if_path_fix`)
- **현재 Claude Code 버전**: v2.1.177 (changelog 미게재 — 신규 check 추가 사유 없음) ✅
- **v2.1.177 gap**: 없음 (changelog 비공개이므로 추적 불필요)

**판정: check 함수 커버리지 정상** ✅

---

## 4. PITFALL 현황

- **총 PITFALL 수**: 259개
- **최근 7일 신규 (2026-07-02~09)**: 1건
  - `pitfall-272` — verification-vs-navigation misclassification  
    (commit `7b39486`: `refactor(harness): remove expect MCP entirely, migrate render-verification to gstack`)

---

## 5. 침묵 의심 발견

### 🔴 [재발] check_v170_fable5_banned() — Issue #11 동일 항목 미수정

**위치**: `harness/scripts/audit-session.sh` L1154–1165

**현재 코드**:
```bash
check_v170_fable5_banned() {
  if grep -qi '"model".*fable\|fable.*"model"' "$settings_file"; then
    echo "FAIL|... 2026-06-21 대표님 금지 지시. 즉시 claude-opus-4-8로 교체 필요"
  else
    echo "PASS|Fable 5 미사용 — v2.1.170 STICKY 금지 준수 (재도입 금지)"
  fi
}
```

**실제 STICKY 결정** (CLAUDE.md 폐기 표):
> `~~2026-06-21~~ → **2026-07-03 금지 해제**` — 대표님 명시: "의도적 재도입이야. fable 5 모델이 되살아났기 때문이야."

**Impact**: Fable 5가 메인 세션 모델로 올바르게 운영되고 있어도 FAIL 발생 → 잘못된 롤백 유도 가능

**Issue #11**: 2026-07-06 생성, 현재까지 OPEN (2026-07-08 마지막 업데이트, 코멘트 2건) — **수정 없음**

---

### 🟡 [신규] check_v173_fable5_suffix_strip() — 거짓 PASS (연관 결함)

**위치**: `harness/scripts/audit-session.sh` L1175–1182

**현재 코드**:
```bash
check_v173_fable5_suffix_strip() {
  if grep -q "Fable.*금지\|fable.*banned\|claude-fable-5.*금지" "$claude_md"; then
    echo "PASS|CLAUDE.md Fable 5 금지 정책 확인 — v2.1.173 [1m] suffix strip 무관 (STICKY 금지 유지)"
  else
    echo "WARN|CLAUDE.md Fable 5 금지 정책 미확인"
  fi
}
```

**문제**:
- CLAUDE.md changelog에 "v2.1.170: **Claude Fable 5 출시** ... ⛔ 우리 사용 금지" 텍스트 존재
- grep `"Fable.*금지"` → 이 라인에 매칭 → **PASS 출력**
- 하지만 PASS 메시지: "STICKY 금지 유지" → **사실과 다름** (2026-07-03 금지 해제됨)

**Impact**: 감사가 Fable 5를 여전히 금지된 것으로 잘못 보고 — 정책 혼동 야기, 감사 신뢰도 저하

---

## 6. 한계 명시 (정적 분석 전용)

- 본 감사는 **정적 파일 분석만** 수행. 런타임 hook 실행 여부는 검증하지 않음
- 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 → 본 감사에서 grep 직접 확인 완료
- hook 파일 존재 = 배포됨을 의미하지 않음. `harness/install.sh` 또는 `deploy.sh` 실행 이력은 미확인

---

## 조치 사항

| 항목 | 유형 | 조치 |
|------|------|------|
| check_v170_fable5_banned | 재발 | Issue #11에 재발 코멘트 추가 |
| check_v173_fable5_suffix_strip | 신규 | 신규 Issue 생성 |

---

*자동 생성 — harness-audit-daily 에이전트 (정적 분석 전용, 2026-07-09 KST 09:00)*
