# JamesClaw 일일 자동 감사 — 2026-06-14

**감사 에이전트**: 일일 자동 감사 (KST 09:00 / UTC 00:00)  
**감사 일시**: 2026-06-14  
**결과**: ⚠️ 침묵 의심 2건 — 2건 모두 **재발** (Issue #4 동일 항목, 4회차)

---

## 1. 사전 — 기존 OPEN Issue 조회

| No | Title | 생성일 | 댓글 수 | 상태 |
|----|-------|--------|---------|------|
| **#4** | [harness-audit-daily] 2026-06-04: 2건 침묵 의심 — check_v144 오류 가이던스 + v2.1.145~158 감사 공백 | 2026-06-04 | 9 | OPEN |

→ 이번 감사 결과와 비교: **2건 모두 동일 재발** (4회차). Issue #4에 코멘트 추가.

---

## 2. settings.json Hook 인벤토리

**등록된 Hook 이벤트 타입:**

| 이벤트 | 등록 hook 수 |
|--------|-------------|
| PreToolUse | 6개 matcher (Write/Edit·Tavily·screenshot·chrome·Bash 5종) → 11개 명령 |
| PostToolUse | 13개 matcher → 27개 명령 |
| SubagentStop | 1개 (`verify-subagent.sh`) |
| UserPromptSubmit | 5개 (`user-prompt.ts`, `user-prompt-declare-warn.sh`, `capture-reset-times.sh`, `pitfall-auto-record.sh`, `build-detector.sh`) |
| PreCompact | 3개 (`pre-compact-snapshot.sh`, `audit-session.sh --compact`, `self-evolve.sh --apply`) |
| Stop | 6개 (`stop-dispatcher.sh`, `session-learning.sh`, `emergency-mode-check.sh`, `inbox-classifier.sh`, `self-evolve-trigger.sh`, `pitfall-auto-record-stop.sh`) |
| StopFailure | 1개 (`telegram-notify.sh stop-failure`) |
| PostCompact | 4개 (`telegram-notify.sh compact`, `session-start.ts`, `post-compact-resume.sh`, `dialectic-pattern-extractor.sh`) |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | 각 1개 inline |
| SessionStart | 8개 (`telegram-notify.sh start`, `emergency-banner.sh`, `session-start-active-infra.sh`, `codex-expiry-check.sh`, `session-start.ts`, `wiki-sync-scheduler.sh`, `harness-self-audit.sh`, `distress-queue-loader.sh`) |
| PostToolUseFailure | 1개 (`tool-duration-monitor.sh`) |

**파일 존재 검증:**

| 항목 | 결과 |
|------|------|
| harness/hooks/ .sh 파일 전체 (약 58개) | ✅ 전원 존재 |
| harness/hooks/session-start.ts | ✅ 존재 |
| harness/hooks/user-prompt.ts | ✅ 존재 |
| harness/scripts/audit-session.sh | ✅ 존재 |
| harness/scripts/self-evolve.sh | ✅ 존재 |

→ **파일 부재 침묵 의심 0건**

---

## 3. 의존성 매트릭스 검증 (P-113)

**P-113 교훈 준수: grep 직접 확인, "같은 파일명 = 같은 path" 가정 금지**

| 소비자 hook | 기대 state 파일 | 공급원 | path 일치 |
|-------------|----------------|--------|-----------|
| `emergency-mode-check.sh` | `$HOME/.harness-state/5h_usage.txt` | `telegram-notify.sh` L9: `STATE_DIR="$HOME/.harness-state"` → L86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` | ✅ 완전 일치 |
| `self-evolve-trigger.sh` | `$HOME/.harness-state/context_usage.txt` | `telegram-notify.sh` L9: `STATE_DIR="$HOME/.harness-state"` → L218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` | ✅ 완전 일치 |
| `pitfall-auto-record-stop.sh` | `$HOME/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` L25: `HARNESS_STATE="${HOME}/.harness-state"` → L80: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | ✅ 완전 일치 |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-{HASH}/build_detected` | `build-detector.sh` L37: `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` → L38: `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` / `enforce-build-transition.sh` L14~15 동일 공식 | ✅ PROJECT_HASH 산출 공식 일치 |

→ **4/4 path 일치** (PROJECT_HASH 산출 공식 포함 전수 grep 확인 완료)

---

## 4. audit-session.sh check_ 함수 분석

**현황:**
- 헤더 명시: `현재 체크 수: 56개 (2026-05-19)`
- grep 실제 집계 (`^check_[a-z]`): **55개**
- 헤더 56 vs grep 55: **1개 불일치** (전 감사와 동일)
- 최신 버전 check: `check_v144_mcp_paginated_tools` (v2.1.144)

**커버리지 공백:**
- v2.1.145 ~ v2.1.173: **29개 버전** 감사 함수 없음 (전 감사 2026-06-13과 동일)

**CLAUDE.md 명시 신규 기능 중 미감사 (전 감사 동일):**

| 버전 | 변경 | 필요 check 함수 |
|------|------|----------------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8 default high effort + `/effort xhigh` | `check_v154_opus48_effort` |
| v2.1.154 | Dynamic Workflows (`/workflows`) opt-in | `check_v154_dynamic_workflows` |
| v2.1.159 | 트리거어 `workflow`→`ultracode` | `check_v159_ultracode_trigger` |
| v2.1.163 | Stop hook `hookSpecificOutput.additionalContext` + if조건 fix | `check_v163_stop_additionalcontext` |
| v2.1.166 | `fallbackModel` 3단계 폴백 + `MAX_THINKING_TOKENS=0` | `check_v166_fallback_model` |
| v2.1.169 | `--safe-mode` hook 격리 + `/cd` cache 보존 + `disableBundledSkills` | `check_v169_safe_mode` |
| v2.1.170 | Claude Fable 5 출시 (`claude-fable-5`) | `check_v170_fable5` |
| v2.1.172 | 서브에이전트 중첩 최대 5단계 + 1M 자동 compact 복구 | `check_v172_nested_subagents` |
| v2.1.173 | Fable 5 `[1m]` suffix 자동 strip — `claude-fable-5`=`claude-fable-5[1m]` | `check_v173_fable5_suffix` |

---

## 5. 침묵 의심 목록

### ⚠️ 침묵 의심 1 (재발 4회차) — check_v144_model_single_session stale 'd' 키 메시지

**파일**: `harness/scripts/audit-session.sh` L1020 (추정)  
**현재 코드**:
```bash
echo "WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
```

**문제**: v2.1.153에서 `d` 키 제거됨.  
CLAUDE.md 명시:
> "별도 `d` 키 불필요 — `d` 액션은 v2.1.153에서 제거됨"  
> "현재 세션만 바꾸려면 picker에서 `s` 키"

**올바른 가이던스**:
```bash
echo "WARN|settings.json model 필드 미명시 — v2.1.153+ /model 영구화(원복). opusplan 고정 시 settings.json model 필드 또는 /model opusplan 1회 호출(자동 저장). 현재세션만 변경 시 picker 's' 키 사용."
```

**이력**: 2026-06-04 최초 → 2026-06-05 재발 → 2026-06-13 재발(3회차) → **2026-06-14 재발(4회차)**

---

### ⚠️ 침묵 의심 2 (재발 4회차, 범위 동일) — v2.1.145~v2.1.173 check 함수 0개

**파일**: `harness/scripts/audit-session.sh`  
**현황**: 마지막 버전 check = v2.1.144. CLAUDE.md는 v2.1.173까지 반영됨.  
**공백**: **29개 버전** (전 감사 2026-06-13과 동일 — 추가 악화 없음)

**CLAUDE.md 감사 동기화 규칙 위반**:
> "감사 항목 동기화 필수: CLAUDE.md에 규칙 추가 또는 Claude Code 버전 업데이트 시, audit-session.sh에 대응하는 check_ 함수도 동시에 추가."

**이력**: 2026-06-04 최초(14버전) → 2026-06-05 재발(14버전) → 2026-06-13 재발(29버전) → **2026-06-14 재발(29버전, 범위 동일)**

---

## 6. PITFALL 현황

- **총 파일 수**: **255개** (전 감사 2026-06-13과 동일 — 신규 없음)
- **최신 번호**: P-262 (`pitfall-262-codex-oauth-token-expired-refresh-rotation-relogin`)
- **최근 7일 신규 커밋 (2026-06-07 ~ 2026-06-14)**:

| 커밋 | 날짜 | 내용 |
|------|------|------|
| e694761 | 2026-06-11 | harness diet 일괄 커밋 — P-224~P-262 범위 다수 파일 신규 등록 |

→ 2026-06-13 이후 신규 pitfall 추가 없음 (git log 확인)

---

## 7. 결론 및 액션

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 | ✅ 0건 부재 |
| 의존성 매트릭스 | ✅ 4/4 pass (P-113 grep 직접 확인) |
| 침묵 의심 총계 | ⚠️ 2건 (재발 4회차) |
| 신규 침묵 의심 | 0건 |
| 범위 악화 | 없음 (29버전 공백 동일 유지) |
| 부가 발견 | check_ 함수 수 헤더 불일치 (header: 56, grep: 55) — 이전 감사 동일 |
| PITFALL | 255개 (전 255개, +0) |

**액션**: Issue #4에 재발(4회차) 코멘트 추가. 범위 동일(v2.1.145~v2.1.173, 29버전) 명시.

---

## 8. 한계 명시

- **정적 분석만**. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈 준수**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료 (4/4 ✅).
- 절대경로 `D:/jamesclew/harness/hooks/` 참조 hook은 Windows 로컬 환경 전용. 원격 컨테이너에서 런타임 검증 불가 (정적 존재 확인만).
- check_ 함수 수 불일치 (56 vs 55): 함수 정의 형식 차이 또는 헤더 미갱신 가능성 — 런타임 감사 (`audit-session.sh --full`) 결과와 대조 권장.
