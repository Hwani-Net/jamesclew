# JamesClaw 일일 자동 감사 — 2026-06-04

**발동**: KST 09:00 (UTC 00:00) / 원격 에이전트  
**결과**: ⚠️ 침묵 의심 **2건** 발견  
**이전 OPEN Issue (harness-audit)**: 0건 → 신규 Issue 생성 대상

---

## 1. 기존 OPEN Issue 조회

`harness-audit` 라벨 OPEN Issue: **0건** (GitHub MCP 조회 완료)

---

## 2. Hook 인벤토리 검증

settings.json 등록 hook 전체 vs `harness/hooks/` 실존 파일 대조.

| 이벤트 | hook 수 | 결과 |
|--------|---------|------|
| PreToolUse | 11개 | ✅ 전원 존재 |
| PostToolUse | 20개 | ✅ 전원 존재 |
| UserPromptSubmit | 5개 | ✅ 전원 존재 |
| Stop | 6개 | ✅ 전원 존재 |
| SessionStart | 8개 | ✅ 전원 존재 |
| SubagentStop | 1개 | ✅ 전원 존재 |
| PreCompact | 3개 (2 hooks + 1 script) | ✅ 전원 존재 |
| PostCompact | 4개 | ✅ 전원 존재 |
| StopFailure | 1개 | ✅ 전원 존재 |
| PostToolUseFailure | 1개 | ✅ 전원 존재 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | inline bash | ✅ 파일 불필요 |

**harness/hooks/ 총 파일**: 63개  
**harness/scripts/ 추가 확인**: `audit-session.sh` ✅, `self-evolve.sh` ✅

**특이사항 (설계 특성, 침묵 의심 아님)**: Stop·SessionStart·UserPromptSubmit 일부 hook이 `bash D:/jamesclew/harness/hooks/...` 절대 경로(Windows 전용)로 등록됨. 해당 파일은 `harness/hooks/`에 전원 존재 확인. 사용자 로컬 Windows PC 전용 설계 — 이전 감사와 동일.

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제: writer의 STATE_DIR 정의와 reader의 STATE_DIR 정의가 PROJECT_HASH 사용 여부까지 일치하는지 grep으로 직접 확인.

| 소비자 hook | state 파일 | 공급원 | 검증 결과 |
|------------|-----------|--------|----------|
| `emergency-mode-check.sh` | `~/.harness-state/5h_usage.txt` | `telegram-notify.sh` L86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` (STATE_DIR=`$HOME/.harness-state`) | ✅ 경로 완전 일치 |
| `self-evolve-trigger.sh` | `~/.harness-state/context_usage.txt` | `telegram-notify.sh` L218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` (STATE_DIR=`$HOME/.harness-state`) | ✅ 경로 완전 일치 |
| `pitfall-auto-record-stop.sh` | `~/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` L80: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` (HARNESS_STATE=`${HOME}/.harness-state`) | ✅ 경로 완전 일치 |
| `enforce-build-transition.sh` | `~/.harness-state/build-{PROJECT_HASH}/build_detected` | `build-detector.sh` L37-42: `PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)`, `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"` | ✅ md5sum 기반 PROJECT_HASH 동일, 경로 완전 일치 |

**결론**: 4개 의존성 모두 PATH 완전 일치 ✅

---

## 4. audit-session.sh check_ 함수 현황 ⚠️

- **현재 함수 수**: **56개** (마지막 업데이트: 2026-05-19, v2.1.144까지 커버)
- **전일 대비 변동**: 없음

### ⚠️ 침묵 의심-1: check_v144_model_single_session 오류 가이던스

- **발견 경위**: 2026-06-04 commit `c6a0211` — CLAUDE.md에 v2.1.153 `/model` 정책 원복 반영됨.  
  `v2.1.144에서 잠깐 '현재 세션만'으로 바뀌었다가 **v2.1.153에서 원복**`; `d 액션은 v2.1.153에서 제거됨`
- **문제**: `audit-session.sh`의 `check_v144_model_single_session` WARN 메시지에 아직 "picker 'd' 키 default 설정 권장" 포함.  
  그러나 `d` 키는 v2.1.153에서 제거됨. harness/settings.json에 `model` 필드 없으므로 매 감사마다 이 오류 WARN 발생.
- **영향**: 사용자가 감사 WARN을 보고 'd' 키 설정 시도 → 실패. 잘못된 가이던스 제공.

```bash
# 현재 (잘못됨):
echo "WARN|settings.json model 필드 미명시 — v2.1.144+ /model 단일세션. opusplan 고정 시 settings.json 또는 picker 'd' 키 default 설정 권장"
# 올바른 가이던스 (v2.1.153 원복):
# "v2.1.153+ /model 이제 다시 영구화. 's' 키로 현재세션만 변경 가능. settings.json model 필드 명시 권장"
```

### ⚠️ 침묵 의심-2: v2.1.145~v2.1.158 check 함수 0개 (감사 공백)

- **발견 경위**: 2026-06-04 commit `c6a0211` — `claude-code-manual.md`에 v2.1.145~v2.1.158 이력 추가.  
  `audit-session.sh`는 v2.1.144까지만 커버, 동시 업데이트 미완료.
- **CLAUDE.md 명시 규칙 위반**: "감사 항목 동기화 필수: Claude Code 버전 업데이트 시, `audit-session.sh`에 대응하는 `check_` 함수도 동시에 추가"
- **누락된 주요 버전 및 하네스 영향**:

| 버전 | 변경 | 하네스 영향 |
|------|------|-----------|
| v2.1.153 | `/model` 영구화 원복 + `d` 키 제거 + `s` 키 신설 (현재세션만) | check_v144 오류 가이던스 → check_v153으로 교체 필요 |
| v2.1.154 | Opus 4.8 (`default high effort`) + `/effort xhigh` + Dynamic Workflows (`/workflows`) | Opus 4.8 배포 영향, Dynamic Workflows hook 인지 필요 |
| v2.1.155~v2.1.158 | (manual 추가됨, 내용 미검토) | 미감사 |

---

## 5. PITFALL 현황

- **총 PITFALL 수**: **211개** (pitfall-001 ~ pitfall-256, 중간 결번 포함)
- **전일 대비 변동**: +1건 (`pitfall-256` 신규, commit `c6a0211` 추가)

| # | 슬러그 | 추가일 | 요약 |
|---|-------|--------|------|
| P-256 | deploy-claude-md-oneway-overwrite-manual-undeployed | 2026-06-04 | deploy.sh 단방향 덮어쓰기로 manual 변경분 배포 누락 (P-173 family) |

**최근 7일 신규 pitfall**: P-256 (1건)

---

## 6. 전일 대비 변경사항

| 항목 | 2026-06-03 | 2026-06-04 | 변동 |
|------|-----------|-----------|------|
| Hook 파일 수 | 63 | 63 | 없음 |
| check_ 함수 수 | 56 | 56 | 없음 (신규 버전 check 미추가 = 문제) |
| PITFALL 수 | 210 | 211 | +1 (P-256) |
| settings.json | 동일 | 동일 | 없음 |
| OPEN Issue | 0 | 0 | 없음 |
| CLAUDE.md | v2.1.144까지 반영 | v2.1.158 반영 | ⚠️ audit-session.sh 미동기화 |

---

## 7. 결론

**침묵 의심 건수: 2건**

| 침묵 의심 | 내용 | 우선순위 |
|----------|------|---------|
| 침묵-1 | `check_v144_model_single_session` WARN에 v2.1.153 제거된 `d` 키 잔존 → 오류 가이던스 | 중 |
| 침묵-2 | v2.1.145~v2.1.158 check 함수 0개 (감사 공백 + CLAUDE.md 동기화 규칙 위반) | 고 |

**권장 조치**: `audit-session.sh`에 `check_v153_model_policy_revert` + `check_v154_opus48_dynamic_workflows` 추가. `check_v144_model_single_session` WARN 메시지 수정 (`d` 키 → `s` 키 가이던스로 교체).

---

## 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- **본 에이전트 역할**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료.
- **원격 환경 제약**: 이 에이전트는 `harness/hooks/` 소스 기준으로 검사. 실제 배포된 `~/.claude/hooks/`는 로컬 PC에 있으며 `harness/deploy.sh` 또는 `install.sh` 실행 여부는 로컬에서만 검증 가능.
