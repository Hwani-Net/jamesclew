# JamesClaw 일일 자동 감사 — 2026-06-01

**발동**: KST 09:00 (UTC 00:00) / 원격 에이전트  
**결과**: ✅ 침묵 의심 **0건** CLEAR  
**이전 OPEN Issue (harness-audit)**: 0건 → close 불필요

---

## 1. 기존 OPEN Issue 조회

`harness-audit` 라벨 OPEN Issue: **0건**

---

## 2. Hook 인벤토리 검증

settings.json 등록 hook 전체 vs `harness/hooks/` 실존 파일 대조.

| 이벤트 | hook 수 | 결과 |
|--------|---------|------|
| PreToolUse | 11개 | ✅ 전원 존재 |
| PostToolUse | 20개 | ✅ 전원 존재 |
| UserPromptSubmit | 5개 | ✅ 전원 존재 |
| Stop | 6개 | ✅ 전원 존재 |
| SessionStart | 8개 | ✅ 전원 존재 |
| SubagentStop | 1개 | ✅ 전원 존재 |
| PreCompact | 3개 (2 hooks + 1 script) | ✅ 전원 존재 |
| PostCompact | 4개 | ✅ 전원 존재 |
| StopFailure | 1개 | ✅ 전원 존재 |
| PostToolUseFailure | 1개 | ✅ 전원 존재 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | inline bash | ✅ 파일 불필요 |

**특이사항**: `session-learning.sh` — git 이력상 P-172 작업 시 수정됨. harness/hooks/에 파일 존재 확인 ✅ (삭제되지 않음)

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제: writer의 STATE_DIR 정의와 reader의 STATE_DIR 정의가 PROJECT_HASH 사용 여부까지 일치하는지 grep으로 직접 확인.

| 소비자 hook | state 파일 | 공급원 | 검증 결과 |
|------------|-----------|--------|----------|
| `emergency-mode-check.sh` | `~/.harness-state/5h_usage.txt` | `telegram-notify.sh` L86: `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` | ✅ `$HOME/.harness-state` 동일 |
| `self-evolve-trigger.sh` | `~/.harness-state/context_usage.txt` | `telegram-notify.sh` L218: `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"` | ✅ `STATE_DIR="$HOME/.harness-state"` 동일 |
| `pitfall-auto-record-stop.sh` | `~/.harness-state/pitfall_pending.json` | `pitfall-auto-record.sh` L80: `PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"` | ✅ `${HOME}/.harness-state` 동일 |
| `enforce-build-transition.sh` | `~/.harness-state/build-{PROJECT_HASH}/build_detected` | `build-detector.sh` L37-42: 동일 `md5sum` 기반 `PROJECT_HASH` | ✅ `$HOME/.harness-state/build-$PROJECT_HASH` 동일 |

**결론**: 4개 의존성 모두 PATH 완전 일치 ✅

---

## 4. audit-session.sh check_ 함수 현황

- **현재 함수 수**: **56개** (기준일 2026-04-29: 39개 → +17개 증가)
- 추가된 함수 (v2.1.119~v2.1.144 대응):
  - `check_v136_hard_deny`, `check_v139_goal_agentview`, `check_v139_hook_args_exec`
  - `check_v141_terminal_sequence`, `check_v142_agents_flags`, `check_v142_fast_mode_opus47`
  - `check_v143_stop_hook_block_cap`, `check_v143_powershell_policy`
  - `check_v144_model_single_session`, `check_v144_mcp_paginated_tools`
  - `check_v119_config_persistence`, `check_design`, 기타 5건

**Claude Code 신버전 누락 여부**: settings.json에 `v2.1.144` 커버리지 확인. `check_v144_model_single_session` (단일 세션 `/model` 정책 변화)·`check_v144_mcp_paginated_tools` 모두 등록 완료. 현재 배포 버전 대비 누락 없음 ✅

---

## 5. PITFALL 현황

- **총 PITFALL 수**: **210개** (pitfall-001 ~ pitfall-229, 중간 결번 포함)
- **최근 7일 신규 항목** (2026-05-27 배치 커밋 기준, `9990626`):
  - P-176~P-181: 리넘버링 (구 P-130~135)
  - P-182~P-229: 신규 48건 — OpenClaw 운영 패턴, Vercel 배포, WSL2 동기화, Discord 채널 분리, 콘텐츠 품질 관련

**최근 7일 주요 신규 PITFALL 요약**:

| # | 슬러그 요약 |
|---|------------|
| P-196 | OpenClaw 채널 분리 + 영상 패턴 7채널 운영 |
| P-197 | WSL2 gateway system/user unit 이중 실행 |
| P-206 | cron fire error → OpenClaw 자동 disabled |
| P-213 | 사용자 결정 분기 명시 (취향·미적·전략) |
| P-217 | AskUserQuestion 다중선택 영구화 |
| P-218 | Sub-agent WSL2 경로 명시 강제 |
| P-222 | Hybrid Sync 아키텍처 (WSL2↔Windows) |
| P-223 | VSL2 vmIdleTimeout 무한 + OpenClaw boot autostart |
| P-224~229 | Vercel 배포 관련 6건 (GitHub 연결, 이메일 불일치 등) |

---

## 6. 결론

**침묵 의심 건수: 0건 — CLEAR**

| 검사 항목 | 결과 |
|----------|------|
| Hook 파일 전원 존재 | ✅ |
| 의존성 매트릭스 path 일치 | ✅ 4/4 |
| audit-session.sh 버전 커버리지 | ✅ v2.1.144까지 |
| PITFALL DB 무결성 | ✅ 210개 |

---

## 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- **본 에이전트 역할**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사에서 grep 직접 확인 완료.
- **원격 환경 제약**: 이 에이전트는 `harness/hooks/` 소스 기준으로 검사. 실제 배포된 `~/.claude/hooks/`는 로컬 PC에 있으며 `harness/deploy.sh` 또는 `install.sh` 실행 여부는 로컬에서만 검증 가능.
