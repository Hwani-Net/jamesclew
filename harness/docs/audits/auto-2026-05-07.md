# 일일 자동 감사 보고서 — 2026-05-07

**실행 시각**: 2026-05-07 KST 09:00 (UTC 00:00)  
**결론**: ✅ 침묵 의심 **0건** — CLEAR

---

## 1. 사전 — 기존 OPEN Issue 조회

| 항목 | 결과 |
|------|------|
| label: harness-audit / state: open | **0건** |

기존 OPEN Issue 없음 → close 대상 없음.

---

## 2. Hook 인벤토리 (settings.json 파싱)

등록 이벤트별 hook 수:

| 이벤트 | hook 수 |
|--------|---------|
| PreToolUse | 10개 명령 (5개 그룹) |
| SubagentStop | 1 |
| PostToolUse | 23 |
| UserPromptSubmit | 5 |
| PreCompact | 5 |
| Stop | 7 |
| StopFailure | 1 |
| PostCompact | 3 |
| SessionStart | 7 |
| ConfigChange / WorktreeCreate / WorktreeRemove / InstructionsLoaded | 4 (인라인 bash) |

**settings.json에서 추출된 참조 파일 수**: 54개 (scripts 포함)

---

## 3. Hook 파일 존재 검증

`harness/hooks/` 또는 `harness/scripts/` 실제 파일 대조:

| 파일 | 존재 여부 |
|------|----------|
| bash-tool-blocker.sh | ✅ |
| build-detector.sh | ✅ |
| capture-reset-times.sh | ✅ |
| change-tracker.sh | ✅ |
| chrome-read-page-guard.sh | ✅ |
| codex-expiry-check.sh | ✅ |
| copilot-api-autostart.sh | ✅ |
| cost-tracker.sh | ✅ |
| curation.ts | ✅ |
| dialectic-pattern-extractor.sh | ✅ |
| emergency-banner.sh | ✅ |
| emergency-mode-check.sh | ✅ |
| enforce-build-transition.sh | ✅ |
| enforce-cross-review.sh | ✅ |
| enforce-review.sh | ✅ |
| error-telegram.sh | ✅ |
| explore-router.sh | ✅ |
| harness-self-audit.sh | ✅ |
| inbox-classifier.sh | ✅ |
| irreversible-alert.sh | ✅ |
| log-filter.sh | ✅ |
| loop-detector.sh | ✅ |
| periodic-audit.sh | ✅ |
| pitfall-auto-record-stop.sh | ✅ |
| pitfall-auto-record.sh | ✅ |
| post-compact-resume.sh | ✅ |
| post-edit-dispatcher.sh | ✅ |
| pre-commit-conventional.sh | ✅ |
| pre-compact-snapshot.sh | ✅ |
| progressive-summarize.sh | ✅ |
| quality-gate.sh | ✅ |
| read-once.sh | ✅ |
| regression-autotest.sh | ✅ |
| regression-guard.sh | ✅ |
| self-evolve-trigger.sh | ✅ |
| session-learning.sh | ✅ |
| session-start.ts | ✅ |
| sonnet-vision-delegate-guard.sh | ✅ |
| stitch-drift-guard.sh | ✅ |
| stop-dispatcher.sh | ✅ |
| task-queue-sort.sh | ✅ |
| tavily-guardrail.sh | ✅ |
| telegram-notify.sh | ✅ |
| test-manipulation-guard.sh | ✅ |
| timeline-logger.sh | ✅ |
| user-prompt-declare-warn.sh | ✅ |
| user-prompt.ts | ✅ |
| verify-deploy.sh | ✅ |
| verify-memory-write.sh | ✅ |
| verify-subagent.sh | ✅ |
| vision-routing-guard.sh | ✅ |
| watchdog-ralph.sh | ✅ |
| wiki-raw-save.sh | ✅ |
| wiki-sync-scheduler.sh | ✅ |
| scripts/audit-session.sh | ✅ |
| scripts/self-evolve.sh | ✅ |

**파일 부재 항목: 없음** ✅

> **참고**: settings.json에 `D:/jamesclew/harness/hooks/` 하드코딩 경로 6개 (emergency-mode-check.sh, inbox-classifier.sh, self-evolve-trigger.sh, emergency-banner.sh, pitfall-auto-record.sh, task-queue-sort.sh). Windows 런타임 환경 전용 — 의도된 설계. 파일 자체는 리포에 존재.

---

## 4. 의존성 매트릭스 검증 (P-111 + P-113 강제)

P-113 교훈: path 일치 = 파일명 일치가 아닌 **전체 경로 + PROJECT_HASH 사용 여부**까지 일치.

| 소비자 hook | 상태 파일 | 공급원 | 검증 결과 |
|-------------|----------|--------|----------|
| emergency-mode-check.sh | `~/.harness-state/5h_usage.txt` | telegram-notify.sh | ✅ MATCH |
| self-evolve-trigger.sh | `~/.harness-state/context_usage.txt` | telegram-notify.sh | ✅ MATCH |
| pitfall-auto-record-stop.sh | `~/.harness-state/pitfall_pending.json` | pitfall-auto-record.sh | ✅ MATCH |
| enforce-build-transition.sh | `~/.harness-state/build-{HASH}/build_detected` | build-detector.sh | ✅ MATCH (PROJECT_HASH 로직 동일) |

### 상세 근거

**emergency-mode-check.sh**:
```
HARNESS_STATE="${HOME}/.harness-state"   # line 8
USAGE_FILE="${HARNESS_STATE}/5h_usage.txt"  # line 9
```
**telegram-notify.sh (공급원)**:
```
STATE_DIR="$HOME/.harness-state"         # line 9
echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"  # line 90
```
→ 경로 `~/.harness-state/5h_usage.txt` 완전 일치 ✅

**self-evolve-trigger.sh**:
```
STATE_DIR="$HOME/.harness-state"         # line 9
CONTEXT_FILE="$STATE_DIR/context_usage.txt"  # line 11
```
**telegram-notify.sh (공급원)**:
```
echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"  # line 236
```
→ 경로 `~/.harness-state/context_usage.txt` 완전 일치 ✅

**pitfall-auto-record-stop.sh**:
```
HARNESS_STATE="${HOME}/.harness-state"   # line 28
PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"  # line 29
```
**pitfall-auto-record.sh (공급원)**:
```
HARNESS_STATE="${HOME}/.harness-state"   # line 25
PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"  # line 82
```
→ 경로 `~/.harness-state/pitfall_pending.json` 완전 일치 ✅

**enforce-build-transition.sh**:
```
PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)  # line 14
STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"  # line 15
```
**build-detector.sh (공급원)**:
```
PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)  # line 37
STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"  # line 38
```
→ md5sum 해시 로직 동일, `~/.harness-state/build-{HASH}/build_detected` 완전 일치 ✅

---

## 5. audit-session.sh check_ 함수 현황

| 항목 | 값 |
|------|---|
| check_ 함수 수 | **39개** |
| 기준 (2026-04-29) | 39개 |
| 변동 | 0 (동일) |

버전별 커버리지:
- v2.1.119 → `check_v119_config_persistence`
- v2.1.120 → `check_v120_powershell_fallback`
- v2.1.121 → `check_v121_post_tool_output`, `check_v121_mcp_always_load`

> CLAUDE.md 최종 문서화 버전은 v2.1.118 (2026-04-23). audit-session.sh는 v2.1.121까지 선반영 — 문서화 지연이나 감사 커버리지 누락은 아님.

---

## 6. PITFALL 현황

| 항목 | 값 |
|------|---|
| 총 PITFALL 파일 수 | **98개** |
| 최고 번호 | pitfall-117 |

### 최근 7일 신규 (2026-04-30 ~ 2026-05-07)

| PITFALL | 제목 | 커밋 날짜 |
|---------|------|----------|
| P-114 | state-file-freshness-not-verified | 2026-05-05 |
| P-115 | repl-only-slash-command-not-cli-automatable | 2026-05-05 |
| P-116 | autocompact-env-vars-leak-to-sonnet | 2026-05-05 |
| P-117 | onedrive-powershell-profile-sync-conflict | 2026-05-05 |

신규 4건 모두 hook 인프라에 영향 없음. 문서 기록 레이어.

---

## 7. 결론

**침묵 의심 0건 — CLEAR**

| 검사 항목 | 결과 |
|----------|------|
| hook 파일 전수 존재 확인 (54개) | ✅ PASS |
| 의존성 매트릭스 path 일치 검증 (4쌍) | ✅ PASS |
| audit-session.sh check_ 함수 수 | ✅ 39개 (기준 충족) |
| 신규 Claude Code 버전 감사 누락 | ✅ 없음 (v2.1.121까지 선반영) |
| 기존 OPEN Issue | 0건 (close 대상 없음) |

---

## 한계 명시

- 정적 분석만. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 본 감사에서 4쌍 모두 직접 grep 검증 완료.
- Windows 하드코딩 경로(D:/jamesclew/)를 사용하는 6개 hook은 Linux 환경에서 실행 불가. 런타임 환경 분리 설계로 의도된 동작.
