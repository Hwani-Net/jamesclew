# 일일 자동 감사 — 2026-05-26

**실행 시각**: 2026-05-26 KST 09:00 (UTC 00:00)  
**결과**: ⚠️ **침묵 의심 1건** — audit-session.sh v2.1.143~v2.1.144 커버리지 갭  
**기존 OPEN Issue (harness-audit)**: 0건

---

## 1. 기존 OPEN Issue 조회

`mcp__github__list_issues` 조회 결과 `harness-audit` 라벨 OPEN Issue **0건**.

---

## 2. Hook 인벤토리 검증

settings.json 파싱 — 등록된 hook 이벤트별 명세:

| 이벤트 | 등록 수 |
|--------|--------|
| PreToolUse | 11개 |
| PostToolUse | 20개 |
| UserPromptSubmit | 5개 |
| SubagentStop | 1개 |
| PreCompact | 3개 |
| Stop | 6개 |
| StopFailure | 1개 |
| PostCompact | 4개 |
| SessionStart | 8개 |
| PostToolUseFailure | 1개 |
| ConfigChange | 1개 |
| WorktreeCreate | 1개 |
| WorktreeRemove | 1개 |
| InstructionsLoaded | 1개 |

**harness/hooks/ 실제 존재 여부**: 모든 등록 hook 파일 확인 ✅

D:/jamesclew/ 하드코딩 경로 hook 6개(emergency-banner, emergency-mode-check, inbox-classifier, self-evolve-trigger, pitfall-auto-record, task-queue-sort) — 원격 Linux 환경에서 미실행 정상 (Windows 로컬 PC 전용, 런타임은 로컬 담당).

미등록 파일(hooks/에 존재하나 settings.json 미등록):
- `awesome-statusline.sh`, `curation.ts`, `enforce-execution.sh`, `evidence-first.sh`, `test-pre-commit-conventional.sh`
- `stop-dispatcher.sh` 내부 호출 방식으로 동작 (등록 불필요)

→ **hook 파일 부재 기반 침묵 의심 없음** ✅

---

## 3. 의존성 매트릭스 검증 (P-111 + P-113)

P-113 강제 — writer/reader 각 hook의 STATE_DIR 정의 직접 grep 확인.

| 소비자 hook | 기대 state 파일 | 공급원 | path 검증 |
|-------------|---------------|--------|----------|
| `emergency-mode-check.sh` | `$HARNESS_STATE/5h_usage.txt` (HARNESS_STATE=`$HOME/.harness-state`) | `telegram-notify.sh` → `$STATE_DIR/5h_usage.txt` (STATE_DIR=`$HOME/.harness-state`) | ✅ 일치 |
| `self-evolve-trigger.sh` | `$STATE_DIR/context_usage.txt` (STATE_DIR=`$HOME/.harness-state`) | `telegram-notify.sh` → `$STATE_DIR/context_usage.txt` (동일 STATE_DIR) | ✅ 일치 |
| `pitfall-auto-record-stop.sh` | `$HARNESS_STATE/pitfall_pending.json` (HARNESS_STATE=`$HOME/.harness-state`) | `pitfall-auto-record.sh` → `$HARNESS_STATE/pitfall_pending.json` (동일 HARNESS_STATE) | ✅ 일치 |
| `enforce-build-transition.sh` | `$HOME/.harness-state/build-$PROJECT_HASH/build_detected` (PROJECT_HASH=`echo "$PWD" | md5sum | cut -c1-8`) | `build-detector.sh` → 동일 STATE_DIR 공식 + 동일 PROJECT_HASH | ✅ 일치 |

**4/4 path 일치 — path mismatch 의심 없음** ✅

---

## 4. audit-session.sh check_ 함수 개수 + 신버전 대응

### 실측 결과

| 항목 | 값 |
|------|-----|
| check_ 함수 정의 수 (`^check_[a-zA-Z_]*()` 기준) | **34개** |
| audit-session.sh 총 라인 수 | 1,224줄 |
| 최종 수정 커밋 | `2272bc4` (v2.1.119~v2.1.142 체크 추가) |

### ⚠️ 침묵 의심 — v2.1.143~v2.1.144 check_ 함수 미추가

CLAUDE.md는 `a909e52 docs(harness): sync v2.1.143~v2.1.144`로 갱신되었으나, audit-session.sh에는 대응 check_ 함수가 추가되지 않음.

**미추가 대상 기능 (CLAUDE.md 등재 완료):**

| 버전 | 기능 | 침묵 가능성 |
|------|------|------------|
| v2.1.143 | Stop hook 8연속 block 자동 종료 (`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`) | `enforce-execution.sh`/`evidence-first.sh` false-positive 연속 발생 시 미감지 |
| v2.1.144 | `/model` 단일 세션 변경 (재시작 시 초기화) | opusplan default 설정 누락 시 다음 세션 정책 위반 미탐지 |
| v2.1.144 | MCP `tools/list` 첫 페이지만 노출 fix | agentmemory 51개 도구 누락 여부 audit 불가 |
| v2.1.143 | `worktree.bgIsolation: "none"` 설정 신설 | 신규 설정 인지 여부 audit 불가 |

### 이전 감사 "56개" 주장 오류 적발

2026-05-25 자동 감사는 `check_v144_model_single_session`, `check_v144_mcp_paginated_tools` 등 함수가 존재한다고 보고했으나, 정밀 grep(`^check_[a-zA-Z_]*()`) 결과 해당 함수는 존재하지 않음. 이전 감사가 함수명을 hallucination한 것으로 판단.

- 실제 현재 check_ 함수: 34개
- 이전 감사 주장: 56개 (오류)
- 2026-04-29 기준: 39개 (일부 GPT-4.1 관련 함수 제거로 현재 34개 — `9e809a2 refactor` commit)

---

## 5. PITFALL 현황

**총 PITFALL 파일 수**: 170개 (최고 번호 P-173, 일부 번호 중복 슬러그 포함)

### 최근 7일 신규 항목 (2026-05-19 ~ 2026-05-26)

| 항목 | 커밋 |
|------|------|
| 신규 없음 (0건) | — |

마지막 신규 PITFALL: `974b239` (P-133/134/135, 2026-05-19 이전)

---

## 6. 결론

| 검증 항목 | 결과 |
|----------|------|
| Hook 파일 전체 존재 | ✅ CLEAR |
| 의존성 매트릭스 4/4 | ✅ CLEAR |
| audit-session.sh v2.1.143-144 커버리지 | ⚠️ 갭 존재 |
| PITFALL 신규 (7일) | ✅ 0건 |

**침묵 의심 1건** — 신규 Issue 생성 (이전 OPEN Issue 없음).

---

## 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당
- **본 에이전트**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어
- **P-113 교훈**: "기능 정상" 결론 시 path 일치까지 검증 의무 — 본 감사에서 이행 ✅
- **D:/jamesclew/ 하드코딩 경로**: 원격 환경에서 미실행. 런타임 검증은 로컬 PC 담당
- **이전 감사 신뢰성**: 원격 에이전트 감사 자체가 함수명 hallucination 위험 있음 — 정밀 grep 기반 재검증 필수 (본 감사에서 `^check_[a-zA-Z_]*()` 기준 사용)
