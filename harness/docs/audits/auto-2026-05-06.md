# Harness Daily Audit — 2026-05-06 (자동 생성)

> 발동: KST 09:00 (UTC 00:00) / 에이전트: JamesClaw Harness Daily Audit Agent  
> 기준 커밋: `583ba0e` (2026-05-06 기준 HEAD)  
> Claude Code 버전: v2.1.128 (어제 기준, 원격 환경 changelog 미접근)  
> 이전 보고서: `auto-2026-05-05.md` (침묵 의심 0건 CLEAR)

---

## 요약

| 항목 | 결과 |
|------|------|
| Hook 파일 존재 검증 | ✅ 전부 존재 |
| 의존성 매트릭스 검증 (4건) | ✅ 전부 공급원 확인 |
| audit-session.sh check_ 개수 | ✅ 39개 (예상치 일치) |
| 피트폴 총 개수 | ✅ 98개 (어제 대비 +2) |
| 최근 7일 신규 피트폴 | P-113~P-117 (5건) |
| **침묵 의심 종합** | **✅ 0건** |

---

## 1. Hook 파일 존재 검증

settings.json 등록 hook 전수 대조 결과 — 전부 harness/hooks/ 또는 harness/scripts/ 내 존재.

```
$HOME/.claude/hooks/ 계열 (harness/hooks/ 보관):
  verify-memory-write.sh, enforce-build-transition.sh, tavily-guardrail.sh,
  vision-routing-guard.sh, chrome-read-page-guard.sh, quality-gate.sh,
  pre-commit-conventional.sh, irreversible-alert.sh, bash-tool-blocker.sh,
  verify-deploy.sh, verify-subagent.sh, periodic-audit.sh, enforce-review.sh,
  error-telegram.sh, loop-detector.sh, timeline-logger.sh, post-edit-dispatcher.sh,
  regression-autotest.sh, enforce-cross-review.sh, change-tracker.sh,
  regression-guard.sh, test-manipulation-guard.sh, read-once.sh,
  sonnet-vision-delegate-guard.sh, explore-router.sh, log-filter.sh,
  cost-tracker.sh, wiki-raw-save.sh, progressive-summarize.sh, stitch-drift-guard.sh,
  watchdog-ralph.sh, user-prompt-declare-warn.sh, capture-reset-times.sh,
  pre-compact-snapshot.sh, stop-dispatcher.sh, session-learning.sh,
  pitfall-auto-record-stop.sh, telegram-notify.sh, wiki-sync-scheduler.sh,
  codex-expiry-check.sh, copilot-api-autostart.sh, post-compact-resume.sh,
  harness-self-audit.sh, build-detector.sh, dialectic-pattern-extractor.sh

TypeScript hooks:
  user-prompt.ts, curation.ts, session-start.ts

D:/jamesclew/harness/hooks/ 직접 참조 (Windows 절대 경로):
  task-queue-sort.sh, pitfall-auto-record.sh, emergency-mode-check.sh,
  inbox-classifier.sh, self-evolve-trigger.sh, emergency-banner.sh

$HOME/.claude/scripts/ 참조:
  audit-session.sh, self-evolve.sh
```

누락 파일: **없음** ✅

---

## 2. 의존성 매트릭스 검증 (P-111 + P-113 기준)

### ✅ 의존성 #1 — `5h_usage.txt` 공급원

- **소비자**: `emergency-mode-check.sh` → `${HARNESS_STATE}/5h_usage.txt`  
  (`HARNESS_STATE="${HOME}/.harness-state"`, line 8–9)
- **공급원**: `telegram-notify.sh` line 90 → `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"`  
  (`STATE_DIR="$HOME/.harness-state"`, line 9)
- **검증**: grep 직접 확인. PROJECT_HASH 미사용 — 양쪽 모두 flat 단일 경로. ✅ PATH 일치.

### ✅ 의존성 #2 — `context_usage.txt` 공급원

- **소비자**: `self-evolve-trigger.sh` → `$STATE_DIR/context_usage.txt`  
  (`STATE_DIR="$HOME/.harness-state"`, line 9)
- **공급원**: `telegram-notify.sh` line 236 → `echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"`  
  (동일 STATE_DIR)
- **검증**: grep 직접 확인. PROJECT_HASH 미사용 — 양쪽 동일. ✅ PATH 일치.

### ✅ 의존성 #3 — `pitfall_pending.json` 공급원

- **소비자**: `pitfall-auto-record-stop.sh` → `${HARNESS_STATE}/pitfall_pending.json`  
  (`HARNESS_STATE="${HOME}/.harness-state"`, line 28)
- **공급원**: `pitfall-auto-record.sh` → `$HARNESS_STATE/pitfall_pending.json`  
  (동일 변수명, line 25+82)
- **검증**: grep 직접 확인. ✅ PATH 일치.

### ✅ 의존성 #4 — `build_detected` 공급원

- **소비자**: `enforce-build-transition.sh`  
  → `PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)`  
  → `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"`  
  → `$STATE_DIR/build_detected` (line 14–16, 38)
- **공급원**: `build-detector.sh`  
  → `PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)`  
  → `STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"`  
  → `DETECTED_FILE="$STATE_DIR/build_detected"` (line 37–42)
- **검증**: md5sum 공식 동일. PROJECT_HASH 사용 여부 양쪽 일치. P-113 교훈 준수. ✅ PATH 일치.

---

## 3. audit-session.sh check_ 함수 개수

| 항목 | 값 |
|------|-----|
| 현재 check_ 함수 수 | **39개** |
| baseline (2026-04-29) | 39개 |
| 차이 | 0 |
| 최상위 버전별 check | `check_v121_post_tool_output`, `check_v121_mcp_always_load` |

**버전 커버리지 분석** (CLAUDE.md 기준):

| 버전 | CLAUDE.md 하네스 영향 | check_ 필요 |
|------|----------------------|-------------|
| v2.1.112 (PowerShell tool) | check_v120_powershell_fallback ✅ | 반영됨 |
| v2.1.116~118 (mcp_tool hook type, /usage, effort 변경) | 선택적 도입 — breaking 변경 없음 | 불필요 |
| v2.1.117 (Opus 4.7 context 계산 수정) | heartbeat 수치 재검증 필요 (정보성) | 불필요 |
| v2.1.119~121 (config persistence 등) | check_v119/v121 계열 ✅ | 반영됨 |
| v2.1.122~128 | ❓ 로컬 매뉴얼 미반영 (원격 환경 한계) | 판단 불가 |

> ℹ️ **정보 (정적 분석 한계)**: v2.1.122~v2.1.128은 원격 환경에서 `~/.claude/cache/changelog.md` 접근 불가. 로컬 매뉴얼 업데이트 후 check_ 추가 여부 수동 검토 권장.

---

## 4. 피트폴 현황

| 항목 | 값 |
|------|-----|
| 총 피트폴 파일 수 | **98개** |
| 어제 대비 증가 | +2 (96 → 98) |
| 번호 범위 최상위 | P-117 |

### 최근 7일 신규 (2026-04-29~2026-05-06)

| 번호 | 슬러그 | 요약 |
|------|--------|------|
| P-113 | path-mismatch-dead-code | `user-prompt.ts` build_detected 경로 불일치 발견·수정. P-113 교훈 → 의존성 #4 검증 의무 신설 |
| P-114 | state-file-freshness-not-verified | stale 상태 파일 신뢰, 컨텍스트 % 오보 |
| P-115 | repl-only-slash-command-not-cli-automatable | `/compact` 외부 자동화 불가, `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` 대체 |
| P-116 | autocompact-env-vars-leak-to-sonnet | autocompact 중 환경변수 Sonnet 세션에 누출 |
| P-117 | onedrive-powershell-profile-sync-conflict | OneDrive Documents 동기화가 PowerShell `$PROFILE` 편집을 덮어씀 |

> P-116, P-117은 오늘 신규 확인 (어제 audit 이후 추가).

---

## 5. 침묵 의심 종합

**침묵 의심: 0건**

정적 분석 범위 내 모든 확인 항목 이상 없음.  
이전 OPEN Issue: 없음 (harness-audit 라벨 기준) → close 대상 없음.

---

## 한계 명시

- **정적 분석만 수행**. 파일 존재·코드 grep 기반. 런타임 실행 결과는 검증하지 않음.
- 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- `~/.claude/cache/changelog.md` 미접근(원격 환경) → v2.1.122~v2.1.128 하네스 영향 판단 불가. 로컬 매뉴얼(`~/.claude/docs/claude-code-manual.md`) 업데이트 및 check_ 추가 여부 검토는 수동 조치 권장.
- **본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어**. P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 오늘 의존성 #1~#4 모두 검증 완료.
