# JamesClaw Harness Full Audit — 2026-05-04

**트리거**: P-111 발견 후 ("코드 존재 ≠ 코드 동작") 전체 검증 요청
**프로토콜**: P-111 4단계 검증 (존재 / 발동 / 분기 / 효과) 모든 영역에 적용
**목표**: 모든 핵심 기능이 의도대로 100% 동작하는지 실증

## 검증 카테고리

| 영역 | 위임 |
|------|------|
| A. Stop / SessionStart / PreCompact hooks | Sonnet Agent A |
| B. UserPromptSubmit / PostToolUse / PreToolUse hooks | Sonnet Agent B |
| C. External Model Integration (Codex / GPT-4.1 / Tavily / Ollama / gbrain) | Sonnet Agent C |
| D. Core Commands / Scripts | Sonnet Agent D |
| E. 외부 모델 종합 검수 | Codex + GPT-4.1 |
| F. 종합 + 수정 적용 | Opus 최종 판단 |

## 4단계 검증 (각 항목)

| 단계 | 의미 | 검증 방법 |
|------|------|----------|
| 1. 존재 | 파일 + 권한 + settings.json 등록 | `ls -la`, `grep` |
| 2. 발동 | 해당 이벤트 트리거 시 실행 흔적 | state 파일 mtime, log |
| 3. 분기 | 핵심 로직 진입 (skip/early-exit 분기 X) | 수동 실행 + 분기 출력 |
| 4. 효과 | 의도한 산출물 생성 (파일/gbrain/텔레그램) | 실제 산출물 검증 |

## 결과 집계

각 Agent는 `~/.harness-state/audit-2026-05-04/agent-{A,B,C,D}.md`에 결과 저장.
Opus가 종합 후 본 파일에 최종 요약 추가.

## 최종 결과 (2026-05-04 종합)

**검증 74개 / PASS 57 (77%) / FAIL 9 (12%) / UNKNOWN 5 (7%) / PARTIAL 3**

### 🔴 Critical 침묵 (P-111 패턴)

| # | 항목 | 진단 | 상태 |
|---|------|------|------|
| 1 | pitfall-auto-record.sh | UserPromptSubmit stdin에 `assistant_response` 부재 → 4/17~5/4 0회 발동. **PITFALL 자동기록 0%** | 🟡 미수정 (옵션 A+B 검수 통과) |
| 2 | enforce-build-transition.sh | `build_detected` state 생성 로직 부재 | 🟡 미수정 |
| 3 | emergency-mode-check.sh | `5h_usage.txt` 공급원 없음 | ✅ **수정 완료** (telegram-notify +1줄, 검증 PASS) |
| 4 | self-evolve-trigger.sh | `context_usage.txt` 공급원 없음 | 🟢 **부분 수정** (수동 트리거 검증 OK, 자동 공급원은 후속) |
| 5 | Perplexity MCP DEAD | settings에 명시되나 미등록 | ✅ **수정 완료** (`@jschuller/perplexity-mcp` 등록, 다음 세션 surface) |
| 6 | feedback-loop-run.sh broken | command가 참조하나 파일 없음 | 🟡 미수정 |

### 외부 모델 100% 일치 우선순위 (Codex + GPT-4.1)

1순위 critical: **#1 pitfall-auto-record** (Option A+B 하이브리드 권장 — UserPromptSubmit 1차 게이트 + Stop 이벤트 transcript 2차 검증)
2순위: #6 feedback-loop-run
후순위: #5 Perplexity (이미 해결), #2 enforce-build-transition

### 메타 hook 아이디어 (외부 모델 합의)

`harness-self-audit.sh`: SessionStart + 주 1회 cron으로 모든 hook의 4단계 (등록/존재/입력/실행) 자동 검증 → 침묵 hook 발견 시 텔레그램 알림. P-111 재발 영구 차단.

## 수정 액션 (이번 세션 적용)

1. ✅ `telegram-notify.sh` line 88 직후 `echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"` 추가
2. ✅ Perplexity MCP 등록: `claude mcp add perplexity -s user -- npx -y @jschuller/perplexity-mcp`
3. ✅ `bash harness/deploy.sh` 적용
4. ✅ emergency-mode-check + self-evolve-trigger 수동 검증 (4단계 PASS)
5. ✅ `session-learning.sh` 수정 (이전, P-111 사건 시점)

## 후속 작업 (이번 세션 완료)

1. ✅ **pitfall-auto-record.sh 재작성** — A+B 하이브리드. UserPromptSubmit 1차 게이트 + 신규 `pitfall-auto-record-stop.sh` 2차 검증 (transcript_path 파싱 + agree 매치)
2. ✅ **build-detector.sh 신설** — UserPromptSubmit에서 build 키워드 감지 → `build_detected` state 자동 생성. enforce-build-transition.sh 차단 정상화
3. ✅ **feedback-loop.md** broken reference TODO 처리 (수동 대안 + 신규 작성 후보 명시)
4. ✅ **harness-self-audit.sh 메타 hook 신설** — SessionStart 발동, 모든 hook 4단계 자동 검증, 침묵 발견 시 텔레그램 + additionalContext 알림. **결과: 54/54 PASS, ISSUES 0**

## 미완료 (minor, 다음 세션)

5. **context_usage.txt 공급원** — heartbeat에 컨텍스트 % 기록 1줄 추가 (self-evolve-trigger 자동 발동용)
6. **deploy.sh 마이너 버그** — `connect-ai-adapter` 디렉토리 `-r` 누락 (cp 경고)

## P-111 영구 차단 메커니즘 (확보)

`harness-self-audit.sh`가 SessionStart에 자동 발동하므로, 어떤 hook이라도 침묵 상태로 빠지면 다음 세션 시작 시 즉시 감지됨. 즉 4/17~5/4처럼 18일간 모르고 지나는 사태 재발 불가.

## 출처

- agent-A.md, agent-B.md, agent-C.md, agent-D.md (~/.harness-state/audit-2026-05-04/)
- Codex (gpt-5.5) + GPT-4.1 (copilot-api) 외부 검수 응답

