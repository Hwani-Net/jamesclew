# JamesClaw 일일 자동 감사 보고서

**날짜**: 2026-06-29 KST 09:00  
**에이전트**: Remote Claude Code (claude-sonnet-4-6)  
**상태**: ⚠️ 침묵 의심 2건 — 재발 (Issue #4 연속)

---

## 1. Hook 인벤토리 검증

settings.json 등록 hook 전부 `harness/hooks/` 또는 `harness/scripts/`에 실존 확인.

| 이벤트 | hook 파일 수 | 결과 |
|--------|------------|------|
| PreToolUse | 10 | ✅ 전부 존재 |
| PostToolUse | 21 | ✅ 전부 존재 |
| UserPromptSubmit | 5 | ✅ 전부 존재 |
| Stop | 6 | ✅ 전부 존재 |
| SessionStart | 7 | ✅ 전부 존재 |
| SubagentStop | 1 | ✅ 존재 |
| PreCompact | 3 | ✅ 존재 (`self-evolve.sh` scripts에 존재 확인) |
| PostCompact | 4 | ✅ 전부 존재 |
| StopFailure | 1 | ✅ 존재 |
| ConfigChange/WorktreeCreate/WorktreeRemove/InstructionsLoaded | inline | ✅ 파일 불필요 |
| PostToolUseFailure | 1 | ✅ 존재 |

**→ 파일 부재로 인한 침묵 의심: 0건 ✅**

---

## 2. 의존성 매트릭스 검증 (P-113 기준)

| 소비자 hook | state 파일 | 공급원 | path 일치 |
|------------|-----------|--------|---------|
| emergency-mode-check.sh | `$HOME/.harness-state/5h_usage.txt` | telegram-notify.sh (`STATE_DIR=$HOME/.harness-state`) | ✅ |
| self-evolve-trigger.sh | `$HOME/.harness-state/context_usage.txt` | telegram-notify.sh 동일 `STATE_DIR` | ✅ |
| pitfall-auto-record-stop.sh | `$HOME/.harness-state/pitfall_pending.json` (`HARNESS_STATE`) | pitfall-auto-record.sh 동일 `HARNESS_STATE` | ✅ |
| enforce-build-transition.sh | `$HOME/.harness-state/build-${PROJECT_HASH}/build_detected` | build-detector.sh (`PROJECT_HASH=$(echo "$PWD" \| md5sum \| cut -c1-8)` 동일) | ✅ |

grep 직접 확인 완료 — "같은 파일명 = 같은 path" 가정 사용 안 함.

**→ 의존성 매트릭스 4/4 ✅ (P-113 패턴 없음)**

---

## 3. audit-session.sh check_ 함수 감사

| 항목 | 값 |
|------|---|
| 현재 check_ 함수 수 | 56개 |
| 마지막 업데이트 | 2026-05-19 |
| 마지막 커버 버전 | v2.1.144 |
| 현재 Claude Code 버전 | v2.1.177 (CLAUDE.md 기준) |
| **감사 공백** | **v2.1.145~177 = 33개 버전** |

### ⚠️ 침묵 의심 1: check_v144_model_single_session WARN 메시지 오류 (재발)

- WARN 메시지에 v2.1.153에서 제거된 `d` 키 언급이 여전히 존재
- v2.1.153에서 `/model` 영구화 원복 + `d` 키 제거 + `s` 키(현재세션) 도입
- Issue #4 (2026-06-04 개설) 이후 미수정

### ⚠️ 침묵 의심 2: v2.1.145~177 check 함수 0개 (재발, 규모 확대)

이전 Issue #4 지적 (v2.1.145~158, 14개 버전) → 현재 **v2.1.145~177 (33개 버전)으로 확대**.

주요 누락 check 함수:

| 버전 | 변경 | 누락 check |
|------|------|-----------|
| v2.1.153 | `/model` 영구화 원복 + `d`→`s` 키 | `check_v153_model_policy_revert` |
| v2.1.154 | Opus 4.8 + `/effort xhigh` + Dynamic Workflows | `check_v154_opus48_effort`, `check_v154_dynamic_workflows` |
| v2.1.159 | `workflow`→`ultracode` 트리거어 변경 | `check_v159_ultracode_trigger` |
| v2.1.163 | Stop hook `additionalContext` + `if:Bash()` 오발동 수정 | `check_v163_hook_additionalcontext` |
| v2.1.166 | `fallbackModel` 배열 + cross-session SendMessage 분리 | `check_v166_fallback_model` |
| v2.1.169 | `--safe-mode` + `/cd` + `disableBundledSkills` | `check_v169_safe_mode` |
| v2.1.170 | Fable 5 출시 (우리: 금지) | `check_v170_fable5_policy` |
| v2.1.172 | 서브에이전트 중첩 최대 5단계 + 1M 자동 compact | `check_v172_nested_subagent` |
| v2.1.176 | 세션 제목 대화 언어 생성 + hook `if` 경로 매칭 수정 | `check_v176_session_title` |

---

## 4. PITFALL 현황

- **총 PITFALL 수**: 258개 (최고 번호: P-271)
- **최근 7일 신규** (git log 기준):
  - P-271: sandbox mount desync from authoritative file (2026-06-29, `6b311f1`)
  - P-262: codex OAuth token expired, re-auth recurrence (2026-06-28, `07252cc`)
  - P-270: Fable 5 banned — broad rename collateral (2026-06-27, `2bf68df`)
  - P-269: Cowork desktop MCP separate from CLI codex MCP server (2026-06-24, `4cfd07a`)

---

## 5. 재발 분류 및 조치

### 재발 확인
Issue #4 제목의 두 항목이 이번 감사에서도 동일하게 발견됨:
- 침묵 의심 1 (check_v144 WARN 오류) → **재발**
- 침묵 의심 2 (감사 공백) → **재발 + 규모 확대** (14→33 버전)

→ **기존 Issue #4에 재발 코멘트 추가** (신규 Issue 미생성)

---

## 한계 명시

- 정적 분석만. 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당.
- 본 에이전트는 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어.
- P-113 교훈: "기능 정상" 결론 시 path 일치까지 검증 의무 — 이번 감사 grep 직접 확인 완료 (4/4 ✅).
- PITFALL 최근 7일 신규 분류는 git log 커밋 날짜 기준 (clone 타임스탬프 불신).
