# 일일 자동 감사 보고서 — 2026-05-13

**감사 일시**: 2026-05-13 KST 09:00 (UTC 00:00)  
**감사 에이전트**: Remote Auto-Audit Agent (claude-sonnet-4-6)  
**결과 요약**: ⚠️ 침묵 의심 1건 — Issue #2 미수정 지속 (5일차)

---

## 1. 기존 OPEN Issue 현황

| # | Title | Created | Updated |
|---|-------|---------|---------|
| #2 | [harness-audit-daily] 2026-05-08: 1건 침묵 의심 — audit-session.sh v2.1.122~v2.1.132 check_ 미동기화 | 2026-05-08 | 2026-05-12 |

---

## 2. Hook 인벤토리 검증

### 2-1. settings.json 등록 이벤트 타입

| 이벤트 | 등록 hook 수 |
|--------|------------|
| PreToolUse | 10개 명령 (5개 파일 hook) |
| SubagentStop | 1 (verify-subagent.sh) |
| PostToolUse | 22개 명령 (20개 파일 hook) |
| UserPromptSubmit | 5개 명령 (5개 파일 hook) |
| PreCompact | 5개 명령 (3개 파일 hook + scripts) |
| Stop | 7개 명령 (7개 파일 hook) |
| StopFailure | 1 (telegram-notify.sh) |
| PostCompact | 3 (telegram-notify.sh, session-start.ts, post-compact-resume.sh) |
| SessionStart | 7 (telegram-notify.sh, emergency-banner.sh, codex-expiry-check.sh, copilot-api-autostart.sh, session-start.ts, wiki-sync-scheduler.sh, harness-self-audit.sh) |
| ConfigChange | 1 (inline) |
| WorktreeCreate | 1 (inline) |
| WorktreeRemove | 1 (inline) |
| InstructionsLoaded | 1 (inline) |
| PostToolUseFailure | 1 (tool-duration-monitor.sh) |

### 2-2. 파일 부재 검사

`harness/hooks/` 파일 59개 전수 확인 결과:

| 검사 항목 | 결과 |
|----------|------|
| 파일 총수 | 59개 (+4 vs 2026-05-12 보고) |
| settings.json 참조 파일 전체 존재 여부 | ✅ PASS — 파일 부재 0건 |

**신규 확인 파일 4개 (2026-05-12 보고 대비 +4)**:
- `awesome-statusline.sh` — statusLine.command 참조, hook 이벤트 아님 (정상)
- `enforce-execution.sh` — Stop hook이나 `stop-dispatcher.sh` 내부에서 직접 호출됨 (간접 등록 ✅)
- `evidence-first.sh` — Stop hook이나 `stop-dispatcher.sh` 내부에서 직접 호출됨 (간접 등록 ✅)
- `test-pre-commit-conventional.sh` — 테스트용 파일, settings.json 직접 등록 불필요 (정상)

**$HOME/.claude/hooks/ 참조 hook (전체 존재 확인)**:
- verify-memory-write.sh, enforce-build-transition.sh, tavily-guardrail.sh, vision-routing-guard.sh, chrome-read-page-guard.sh, quality-gate.sh, pre-commit-conventional.sh, irreversible-alert.sh, bash-tool-blocker.sh, verify-deploy.sh, verify-subagent.sh, periodic-audit.sh, enforce-review.sh, error-telegram.sh, loop-detector.sh, timeline-logger.sh, post-edit-dispatcher.sh, regression-autotest.sh, enforce-cross-review.sh, change-tracker.sh, regression-guard.sh, test-manipulation-guard.sh, read-once.sh, sonnet-vision-delegate-guard.sh, explore-router.sh, log-filter.sh, cost-tracker.sh, wiki-raw-save.sh, progressive-summarize.sh, stitch-drift-guard.sh, watchdog-ralph.sh, tool-duration-monitor.sh, user-prompt.ts, user-prompt-declare-warn.sh, capture-reset-times.sh, build-detector.sh, pre-compact-snapshot.sh, curation.ts, stop-dispatcher.sh, session-learning.sh, pitfall-auto-record-stop.sh, dialectic-pattern-extractor.sh, telegram-notify.sh, session-start.ts, post-compact-resume.sh, codex-expiry-check.sh, copilot-api-autostart.sh, wiki-sync-scheduler.sh, harness-self-audit.sh

**D:/jamesclew/harness/hooks/ 참조 hook (전체 존재 확인)**:
- emergency-mode-check.sh, inbox-classifier.sh, self-evolve-trigger.sh, pitfall-auto-record.sh, emergency-banner.sh, task-queue-sort.sh

---

## 3. 의존성 매트릭스 검증 (P-113 강제 — path 일치 grep 직접 확인)

| 소비자 hook | 기대 state 파일 | 공급원 위치 | 검증 결과 |
|-------------|---------------|------------|---------|
| emergency-mode-check.sh | `$HOME/.harness-state/5h_usage.txt` | telegram-notify.sh | ✅ PASS |
| self-evolve-trigger.sh | `$HOME/.harness-state/context_usage.txt` | telegram-notify.sh | ✅ PASS |
| pitfall-auto-record-stop.sh | `$HOME/.harness-state/pitfall_pending.json` | pitfall-auto-record.sh | ✅ PASS |
| enforce-build-transition.sh | `$HOME/.harness-state/build-{PROJECT_HASH}/build_detected` | build-detector.sh | ✅ PASS |

### 검증 근거 (grep 직접 확인)

**emergency-mode-check.sh**:
```
HARNESS_STATE="${HOME}/.harness-state"
USAGE_FILE="${HARNESS_STATE}/5h_usage.txt"
```
**telegram-notify.sh (writer)**:
```
STATE_DIR="$HOME/.harness-state"
echo "$FIVE_INT" > "$STATE_DIR/5h_usage.txt"
```
→ 경로 일치 ✅

**self-evolve-trigger.sh**:
```
STATE_DIR="$HOME/.harness-state"
CONTEXT_FILE="$STATE_DIR/context_usage.txt"
```
**telegram-notify.sh (writer)**:
```
echo "$CTX_PCT" > "$STATE_DIR/context_usage.txt"
```
→ 경로 일치 ✅

**pitfall-auto-record-stop.sh**:
```
HARNESS_STATE="${HOME}/.harness-state"
PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"
```
**pitfall-auto-record.sh (writer)**:
```
HARNESS_STATE="${HOME}/.harness-state"
PENDING_FILE="$HARNESS_STATE/pitfall_pending.json"
```
→ 경로 일치 ✅

**enforce-build-transition.sh**:
```
PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)
STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"
[ ! -f "$STATE_DIR/build_detected" ]
```
**build-detector.sh (writer)**:
```
PROJECT_HASH=$(echo "$PWD" | md5sum | cut -c1-8)
STATE_DIR="$HOME/.harness-state/build-$PROJECT_HASH"
DETECTED_FILE="$STATE_DIR/build_detected"
```
→ PROJECT_HASH 산출 방식(md5sum) + path 구조 완전 일치 ✅

---

## 4. audit-session.sh check_ 함수 현황

| 항목 | 수치 |
|------|------|
| 현재 check_ 함수 수 | **39개** |
| 2026-04-29 기준치 | 39개 |
| 변동 | 0 (동일) |
| 최고 커버 버전 | v2.1.121 (check_v121_mcp_always_load) |

### Claude Code 신버전 대응 현황

| 버전 범위 | check_ 존재 여부 |
|-----------|---------------|
| ~v2.1.121 | ✅ 커버됨 |
| v2.1.122 | ❌ 미추가 |
| v2.1.128 | ❌ 미추가 |
| v2.1.129 | ❌ 미추가 |
| v2.1.132 | ❌ 미추가 |

**→ [AUDIT-001] 침묵 의심: Issue #2 항목 미수정 지속 (2026-05-08 최초 발견 후 5일 경과)**

---

## 5. PITFALL 현황

| 항목 | 수치 |
|------|------|
| 총 PITFALL 수 | **98개** |
| 최신 번호 | P-117 (onedrive-powershell-profile-sync-conflict) |
| 최근 7일 신규 (2026-05-06~05-13) | **0건** (git log 확인 — pitfalls/ 커밋 없음) |

---

## 6. 침묵 의심 상세

### [AUDIT-001] audit-session.sh v2.1.122~v2.1.132 check_ 함수 미추가 (재발 #5일차)

- **최초 발견**: 2026-05-08 (Issue #2 생성)
- **오늘 상태**: 미수정. 39개 count 동일, v2.1.121 이후 check_ 없음
- **위험도**: 신규 Claude Code 기능 감사 사각지대 지속
- **수정 방법**: Issue #2 본문의 check_v132_*, check_v128_* 함수 추가 필요

---

## 7. 감사 요약 매트릭스

| 검사 항목 | 결과 |
|----------|------|
| hook 파일 전수 존재 (59개, 참조 55개) | ✅ PASS |
| 의존성 매트릭스 path 일치 (4쌍) | ✅ PASS |
| audit-session.sh check_ 함수 수 | ⚠️ 39개 (v2.1.122+ 누락) |
| 기존 OPEN Issue | 1건 (Issue #2) |
| 이번 침묵 의심 | 1건 (Issue #2 동일 항목 지속) |
| 신규 PITFALL (7일) | 0건 |

---

## 8. 한계 명시

- **정적 분석만**: 로컬 PC SessionStart hook 또는 Windows Task Scheduler가 런타임 검증 담당
- **본 에이전트 역할**: 코드 변화로 인한 침묵 가능성 사전 감지하는 보완 레이어
- **P-113 교훈 준수**: "기능 정상" 결론 시 path 일치까지 검증 완료 (4쌍 grep 직접 확인)
- **mtime 신뢰도 한계**: CI 환경 git clone 특성상 파일 mtime = checkout 시점. 실제 PITFALL 생성일은 git log로만 정확 확인 가능

---

*보고서 생성: Remote Auto-Audit Agent | harness/docs/audits/auto-2026-05-13.md*
